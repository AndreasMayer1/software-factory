# Protocol: TASK-PROC-026-07 — Extract Initial T1/T2 Design Rules

## 2026-03-01

**Agent**: code-simple orchestrator (claude-sonnet-4-6)
**Agent ID**: session-2026-03-01-task-proc-026-07
**Action**: Created 7 persona-derived design rule files in `doc/presentation/design/`
**Outcome**: PASS — All 7 files created and verified by quality-checker agent

### Files Created

| File | Tier | Status | Token Status |
|------|------|--------|--------------|
| `t1_touch_targets.md` | T1 | Created ✅ | Uses `ComponentTokens.buttonMinHeight` (existing) |
| `t1_dark_mode.md` | T1 | Created ✅ | ⚠️ Flags 2 new tokens needed |
| `t1_interaction_budget.md` | T1 | Created ✅ | No token (behavioral rule) |
| `t1_input_scaffolding.md` | T1 | Created ✅ | No token (pattern rule) |
| `t1_discrete_identity.md` | T1 | Created ✅ | No token (visual direction) |
| `t2_crisis_mode_targets.md` | T2 | Created ✅ | ⚠️ Flags `ComponentTokens.buttonCrisisMinHeight` (64.0) needed |
| `t2_destructive_actions.md` | T2 | Created ✅ | Uses `SpacingTokens.md` (existing) |

### Quality Check Results

All 7 files passed verification:
- ✅ Correct t1_/t2_ naming prefixes
- ✅ WHAT + HOW + WHY structure in every file
- ✅ Specific PERSONA-IDs with trait context
- ✅ Provenance markers present
- ✅ Scope definitions with includes/excludes
- ✅ T2 files document T1 relationships
- ✅ Token strategy correct (reference existing or flag missing)
- ✅ Consistent template across all 7 files

### New Tokens Required (Follow-Up)

Two files flag tokens that don't yet exist in `lib/config/theme/tokens.json`:
1. `t1_dark_mode.md`: `semantic.surface.darkModePrimary: #000000` + `semantic.surface.darkModeElevated: rgba(255,255,255,0.04)`
2. `t2_crisis_mode_targets.md`: `component.button.crisisMinHeight: 64.0`

These are flagged within the rule files but require a separate `doc-update-tokens` task to add to the token system.

**Next Step**: Run `task-complete` skill to mark TASK-PROC-026-07 as done, then commit.
