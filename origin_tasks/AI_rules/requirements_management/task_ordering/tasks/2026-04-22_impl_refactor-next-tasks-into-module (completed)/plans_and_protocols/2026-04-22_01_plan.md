# Plan: Refactor next_tasks.py into task_ordering Module

**Task**: TASK-PROC-042-03
**Date**: 2026-04-22

## Module Split

| Module | Contents |
|--------|----------|
| `defaults.py` | `TERMINAL_STATUSES`, `EXCLUDED_STATUSES`, `_parse_semver`, `priority_score`, `make_sort_key` (factory returning sort_key closure) |
| `dependencies.py` | `is_blocked` |
| `ranker.py` | `rank_tasks`, `rank_tasks_by_package`, `find_next_release`, `find_next_package`, `_requirements_in_progress`, `_requirements_in_progress_by_package` |
| `classifier.py` | Stub only (TASK-PROC-042-05) |
| `rules.py` | Stub only (TASK-PROC-042-04) |
| `__init__.py` | Public API re-exports |

## What stays in next_tasks.py

- `argparse` CLI
- `PROJECT_ROOT`, `RELEASES_FILE`, `RELEASE_BACKLOG_FILE`
- `PRIORITY_OVERRIDE_FILE`, `load_priority_override`
- All data loading: `parse_frontmatter`, `_parse_simple_yaml`, `_parse_scalar`, `load_tasks`, `load_backlog_packages`, `load_active_release`, `_find_files`, `_task_name`
- Output: `_format_task`, `_print_release_warning`
- Release boundary checks (uses `_parse_semver` → import from module)
- AC coverage subprocess check
- `main()`

## Interface contract

`next_tasks.py` imports from `scripts.task_ordering` (absolute) or uses `sys.path` insert for running as script.

All existing `_is_blocked` calls → `is_blocked` from module (same signature).
`TERMINAL_STATUSES`, `EXCLUDED_STATUSES` → imported from module.
`rank_tasks`, `rank_tasks_by_package`, `find_next_release`, `find_next_package` → imported from module.
`_parse_semver` (used in release boundary checks) → imported from module as `parse_semver`.
