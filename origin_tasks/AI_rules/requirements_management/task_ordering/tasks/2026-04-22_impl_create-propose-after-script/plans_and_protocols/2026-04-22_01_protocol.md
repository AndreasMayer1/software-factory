# Protocol: TASK-PROC-042-08 — Create propose_after.py

## Approach: inline implementation

## Implementation

- Single file: `scripts/propose_after.py`
- Imports `load_tasks` from `next_tasks` (safe — only function/constant defs at module level)
- Imports `classify_layer` from `task_ordering.classifier`
- Imports `load_rules` from `task_ordering.rules`
- `extract_scope_key` follows §4.1 pseudocode exactly
- Heuristic matching dispatches by `name` field (5 named heuristics in rules file)
- Deduplication via `seen_ids` set; terminal-status tasks excluded
- Empty output exits 0 (not an error)

## Heuristics implemented

| name | when | selection logic |
|---|---|---|
| same_scope_upstream_layer | any | candidate layer order < new; scope values equal |
| requirement_then_implementation | implementation layer | candidate in req_exploration/derivation, same parent_req |
| scribble_then_implementation | implementation layer | candidate in ui_design, same parent_req |
| flow_approval_then_derivation | requirement_derivation layer | candidate in user_flow, flow name appears in source_gap |
| bundle_explores_then_verification | flow_verification layer | candidate in req_derivation, same verification_bundle |

## Status: complete

## 2026-04-22T18:35:44Z
**Agent**: Main conversation (task-resolve skill)
**Agent ID**: aa27cb45f440e26c6
**Action**: Implemented `scripts/propose_after.py` — heuristic dependency detector CLI
**Outcome**: Pass — all 6 ACs verified:
  - AC1: runs on any existing task path without error ✓
  - AC2: proposes TASK-FUNC-015-01 for impl task with same parent_requirement ✓
  - AC3: terminal-status tasks excluded via TERMINAL_STATUSES filter ✓
  - AC4: output format "TASK-ID   reason" verified ✓
  - AC5: empty output exits 0 ✓
  - AC6: no LLM calls, no file writes (code inspection) ✓
**Next Step**: task-complete
