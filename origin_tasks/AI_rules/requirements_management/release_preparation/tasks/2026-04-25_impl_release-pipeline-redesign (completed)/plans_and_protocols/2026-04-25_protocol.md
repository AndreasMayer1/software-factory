# Protocol: TASK-PROC-035-08 — Distributed Release Pipeline Redesign

---

## 2026-04-25T00:00:00
**Agent**: architecture-advisor (claude-switch-opus planning phase)
**Agent ID**: (session agent — see most recent agent-a*.jsonl in /home/vscode/.ccs)
**Action**: Produced Group 3 implementation-ready plan covering:
  - `scripts/create_orchestration_task.py` — new `_build_after_field()` and `_build_step1_ac()` helpers; full replacement `_GOAL_TEMPLATE` with `plan_path`, `task_type`, `after_field`, `step1_ac` interpolation keys; `task_type` parameter added to `create_orchestration_task()`; Step 3b reads `task_type` from plan via `parse_task_creation_plan.py`; `--task-type` CLI flag added to `main()`
  - `.claude/skills/task-create-code/skill.md` — Phase 0 plan-mode override block (after step 7, before Phase 1); Phase 6 plan conformance check (after 4.2, before Output); two new Automated Mode table rows; two new "not safe" bullets
**Outcome**: Pass — plan written to `plans_and_protocols/plan_group3.md`
**Next Step**: Wait for Group 2 commit, then implement Group 3 using plan_group3.md as sole reference. Implementation agent does NOT need to re-read protocol.md from the explore task.

## 2026-04-25T01:00:00
**Agent**: architecture-advisor (claude-switch-opus planning phase — Group 2)
**Agent ID**: (session agent — see most recent agent-a*.jsonl in /home/vscode/.ccs)
**Action**: Produced Group 2 implementation-ready plan covering:
  - `scripts/create_orchestration_task.py` — 6 changes: (1) `--dry-run` flag skips steps 4–6, same exit codes; (2) `--after-task TASK-ID` appends to `after:` in template; (3) `--plan-path PATH` adds `plan_path:` to template frontmatter; (4) `--task-type TYPE` controls Step 1 AC text via `_STEP1_BY_TYPE` dict; (5) Exit-3 retired — when all packages covered, creates validation orchestration task with structural-check ACs instead; (6) `fcntl.flock` concurrency lock on `.create_orchestration_task.lock`; also adds `_VALIDATION_GOAL_TEMPLATE`, updates `main()` to use `parse_args()`, splits function into `create_orchestration_task()` (lock wrapper) + `_create_orchestration_task_locked()` (inner logic)
  - `.claude/skills/release-begin-impl/skill.md` — full rewrite: removed old Phases 3/4/5; kept/updated Phases 0/1/2/2b; added Phase 2c (Task Creation Planner with large-release splitting at 100KB); repurposed Phase 5 as user gate using `summarize_plan.py`; added Phase 6 full activation sequence (steps 6.0–6.6 + failure recovery table); added Decision Domains table (3 domains: Scope/Coverage/Investigation); added introductory note; Phase 1 calls `should_use_agents.py`; Phase 2b uses output-file polling (no `_agent_state.md`)
**Outcome**: Pass — plan written to `plans_and_protocols/plan_group2.md`
**Next Step**: Implement Group 2 using plan_group2.md as sole reference. Group 2 depends on Group 1 scripts existing (should_use_agents.py, summarize_plan.py). Implementation agent does NOT need to re-read the explore task protocol.md.

## 2026-04-25T13:00:00
**Agent**: architecture-advisor (Opus, via claude-switch-opus — Group 1)
**Agent ID**: (session agent — see most recent agent-a*.jsonl in /home/vscode/.ccs)
**Action**: Produced Group 1 implementation-ready plan covering all 7 new pipeline scripts:
  1. `scripts/parse_task_creation_plan.py` — shared library; parsing algorithm for task_creation_plan.md schema; multi-version handling; public API (`parse_plan`, `get_task_entry`, `get_package_tasks`, `get_execution_order`); exception types `PlanParseError`, `PlanArchivedError`
  2. `scripts/check_task_against_plan.py` — conformance checker; exit 0/1/2/3; `check_conformance()` with effort ±1 WARN logic; finds goal.md by scanning for task_id
  3. `scripts/reconcile_after_chains.py` — after-chain reconciler; `--apply` in-place edit with atomic `os.replace()`; `#PKG-X:Task N` reference resolution; auto-detect plan path
  4. `scripts/summarize_plan.py` — 1-page stats summary; after-chain depth via DAG; effort distribution table
  5. `scripts/check_requirement_implementation.py` — grep-based per-AC verdict (`likely_implemented`/`uncertain`/`likely_missing`); search term generation from AC text; `--json` output
  6. `scripts/find_orchestration_tasks.py` — structural signature detection (`target_release` set + `scope_description` starts with "Orchestration:"); importable module API
  7. `scripts/should_use_agents.py` — release → packages → requirements file size; 30KB/5-file threshold; JSON output with `verdict`, `total_bytes`, `file_count`, `files`
  Pre-implementation check: next_tasks.py target_package→release resolution is CORRECT; no fix needed before Group 1.
**Outcome**: Pass — plan written to `plans_and_protocols/plan_group1.md`
**Next Step**: Implementation agent reads `plan_group1.md` (self-contained). Implement in order: Script 1 + standalone scripts first, then scripts depending on Script 1. Run `python3 -m py_compile` on each after writing. Group 1 is prerequisite for Groups 2, 3, 4.
