# User Initial Input — Reactive Restructuring Analysis

Raw user input preserved verbatim. Read as a seed bed, not a spec.

---

new requ next to an existing one. Both are details of a greater concept: a epic could be created to bundle them.

new ACs to an existing requ. It grows so big that it should be split in multiple features, then an epic should be created to bundle those new features.

a new requ makes an existing requ obsolete. The old one should be marked accordingly.

---

Context from the conversation that prompted this task:

> "What the skill currently also doesn't support is a requ reorganization when new information is added
> that should trigger restructuring. For example (incomplete list):
> - new requ next to an existing one. Both are details of a greater concept: a epic could be created to bundle them.
> - new ACs to an existing requ. It grows so big that it should be split in multiple features, then an epic should be created to bundle those new features.
> - a new requ makes an existing requ obsolete. The old one should be marked accordingly
> - ...
>
> We have a task to restructure the requirements. I want to do this requ modification improvement together
> with the restructuring. It's an enforcement of a clean structure. Check what such a restructuring mechanism
> implies for the existing tasks (and requ) that describe this new structure. Do you think we can and should
> modify those? Alternatively we could also create an additional task there to explore such a mechanism.
> But it shall land after (or together) with the restructuring."
