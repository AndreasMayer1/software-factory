# Protocol — TASK-PROC-049-08

## 2026-05-16 16:30 CEST

**Agent**: Main session (orchestrator, inline)
**Agent ID**: c3480d6b-9551-4613-a28c-a838f12bb370
**Skill**: task-resolve
**Action**:
1. Authored `requirements_user_needs/concept_canon/README.md` (83 lines) — the five required sections (Role / Workflow / Audience axis / Graceful-degradation / Extension ladder) plus file inventory, what-belongs / what-doesn't-belong, ID convention `CONCEPT-<UPPER-KEBAB>`, and Related Requirements. Audience-axis section carries the explicit Self-User note (lay register, persona-shaped not register-shaped distinction). Workflow table covers Cascade A/B/C and the >10-files safety valve. Extension ladder table maps concrete triggers (file size, finding volume, third language, code-coverage maturity, schema migration) to strictly additive next rungs.
2. Updated `CLAUDE.md`:
   - §10 Information Map: added row `Concept canon (user-facing names) | requirements_user_needs/concept_canon/`.
   - §11 Generated Files table: added rows for `concept_canon.md` and `concept_canon.index.yaml` (both generated by `scripts/user_needs/generate_concept_canon_md.py`).
   - §11 script reference: added a new subsection `Concept canon scripts (REQ-PROC-049)` listing `check_canon.py` (audit walker with empty-canon silent-pass behaviour) and `generate_concept_canon_md.py` (regenerator).
3. Cross-reference updates (modification mode, Scenario E of `requ-explore` — targeted Related Requirements additions only, no body restructuring):
   - **REQ-PROC-046** — added `Related Requirements` bullet linking REQ-PROC-049 and noting the shared `.arb` parser under `scripts/quality/` used by both the G6 linguistic-complexity sub-check and `check_canon.py`. Closes the bidirectional link.
   - **REQ-PROC-045** — added `Related Requirements` bullet linking REQ-PROC-049 as the language-layer sibling above the structural layer. Closes the bidirectional link.
   - **REQ-NFUNC-013** — no change. §8.4 + §11 already list REQ-PROC-049; no separate `## Related Requirements` section exists, so per goal AC nothing to add.
   - **REQ-PROC-050** — no change; already bidirectional (verified 2026-05-15 per goal note).
   - **REQ-PROC-002** (scan target) — no canon overlap; test-quality requirement does not touch user-facing language. No link added.
4. Regenerated `requirements_tasks/STATUS.md` via `scripts/artifacts/generate_status_overview.py` (144 reqs, 402 tasks, 11 releases).

**Outcome**: Pass. All acceptance criteria met (README sections + audience axis with self-user note + graceful-degradation + extension ladder + CLAUDE.md §10/§11 + REQ-PROC-046 and REQ-PROC-045 cross-refs via requ-explore + REQ-NFUNC-013/REQ-PROC-050 verified no-action + REQ-PROC-002 scanned + status overview regenerated).

**Notes**:
- Canon-completion gate now closed. The ten Tier-1 back-pressure tasks listed in goal.md `blocks:` (TASK-PROC-046-03/-04/-05/-10, TASK-PROC-052-01/-03, TASK-PROC-002-02/-03/-08/-09) can resume once this task lands.
- `Edit` tool reported transient `ENOENT` warnings on CLAUDE.md twice during this session; in both cases the edit was applied successfully (verified by grep). No data loss; behaviour was idempotent.

**Next Step**: Run `task-complete` to mark TASK-PROC-049-08 done, commit, and refresh status. Subsequent orchestration step picks up next ready task.
