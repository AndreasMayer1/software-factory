# Plan: TASK-PROC-049-07 — Extend Caller Skills + product-intake

## Objective
Wire `ux-write-canon-concept` into 8 caller skills so no user-facing noun/verb/state is introduced without consulting the canon.

## Approach: Inline via claude-modify-skill × 8

Each change is minimal and targeted. No architectural investigation needed — the goal.md specifies exact text per skill.

## Skill Changes (in execution order)

### 1. requ-explore
Location: end of Phase 2.2 "Write Requirements", after "Then write requirement file(s)..."
Add:
> **Canon check**: When writing introduces a user-facing noun/verb/state not in `concept_canon.yaml`, invoke `ux-write-canon-concept`.

### 2. ux-create-flow
Location: end of skill, after "## Guard Rails" section
Add new section:
```
## Canon Check (pass-through)
When new step labels introduce a user-facing noun/verb/state not in `concept_canon.yaml`, pass this in args to `ux-flow-draft` so it invokes `ux-write-canon-concept`.
```

### 3. ux-write-scenario
Location: between step 8 (Generate/Modify) and step 8.5 (User Approval Gate)
Add:
```
## 8.1. Canon Check (FUTURE-state scenarios only)
Invoke `ux-write-canon-concept` ONLY for FUTURE-state scenarios (App-Nutzung) when the scenario introduces a user-facing noun/verb/state not in `concept_canon.yaml`. NEVER for AS-IS (pre-app) scenarios.
```

### 4. code-simple
Location: step 3 "Implement", agent bullet list, after design-token bullet
Add:
> - **Canon alignment**: prefer canonical names from `concept_canon.yaml` for classes, methods, BLoC events/states, user-facing strings; when a new user-facing concept is introduced, invoke `ux-write-canon-concept` first

### 5. code-complex
Location: step 5b "Implementation batches", agent bullet list, after design-token bullet
Add same bullet as code-simple.

### 6. ui-create-scribble
Location: Phase 1 MUST-DO list, after item 14
Add item 15:
> 15. **Canon labels**: Consult `requirements_user_needs/user_flows/concept_canon.yaml`. Use canonical label text for known concepts; annotate with `<!-- canon: CONCEPT-X -->`.

### 7. ui-create-scribble-improve
Location: Sub-agent A task description, after "Generate a scribble into ..."
Add:
> Also: consult `concept_canon.yaml` for label alignment; annotate canonical labels with `<!-- canon: CONCEPT-X -->`.

### 8. product-intake
Location: between Step 4 and Step 5
Add new section:
```
## Canon Impact (Step 4a)
Check whether the proposed change introduces or renames any user-facing concept (noun, verb, or state). If yes → invoke `ux-write-canon-concept` before continuing to Step 5.
```
Also update Progress Report to add:
`Canon impact: checked | invoked ux-write-canon-concept | skipped`

## Execution Order
Sequential (each claude-modify-skill syncs INDEX.md + factory_flows.md — no parallel to avoid conflicts).

## Success Criteria (from goal.md AC)
- [ ] All 8 skill files modified via claude-modify-skill
- [ ] Each modification is minimal
- [ ] ux-write-scenario excludes AS-IS scenarios
- [ ] ui-create-scribble* include <!-- canon: CONCEPT-X --> marker convention
- [ ] product-intake has Canon Impact step between Step 4 and Step 5
