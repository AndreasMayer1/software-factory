# Protocol: TASK-PROC-030-10 — requ-assign-packages Skill + Package Assignment Pipeline

## 2026-04-03 — Plan Review Phase

**Agent**: architecture-advisor (Opus planning via claude-switch-opus)
**Agent ID**: (parent session agent)
**Action**: Reviewed goal.md for all 5 deliverables; read sync_task_packages.py, release-plan/skill.md, requ-derive-from-flow/skill.md, RELEASE_BACKLOG.md; analyzed heuristic signals, edge cases, and acceptance criteria completeness.
**Outcome**: Pass — plan review written to `plans_and_protocols/2026-04-03_01_plan_review.md`

**Key Findings**:
1. Gap scanner needs new `parse_unassigned_items()` function — existing `parse_item_package_map` returns only assigned items
2. Signal 1 (`release_chunk`) has zero current matches in codebase — pure future-proofing
3. Fuzzy matching should be explicitly stated as LLM semantic judgment (not algorithmic)
4. AC `text:` field absent on many requirements — scanner must handle gracefully
5. Deliverable 4 missing loop-return consideration after Action 6 completes
6. ~39 fully-unassigned requirements, most are REQ-PROC-* (intentionally unpackaged)
7. 6 acceptance criteria gaps identified and proposed

**Recommended Execution**: Single agent, 5 phases: gap scanner script → release-plan edits → derive-from-flow edits → skill creation → verification

**Next Step**: User reviews plan at `plans_and_protocols/2026-04-03_01_plan_review.md`, approves or requests changes, then implementation agent executes.

---

## 2026-04-03 — Implementation Phase

**Agent**: implementation-engineer (claude-sonnet-4-6)
**Agent ID**: a58145894644a3818
**Status**: Complete

### Files Created

1. `scripts/sync_requirement_packages.py` (~175 lines)
   - Read-only gap scanner
   - New `parse_unassigned_items()` function — returns items WITHOUT `target_package`
   - Reuses `split_frontmatter()` helper (copied, not imported, to keep script self-contained)
   - Handles missing `text:` field gracefully (shows "(no description)")
   - Handles empty `trackable_items`, missing frontmatter
   - CLI: `python3 scripts/sync_requirement_packages.py [--requirement PATH]`

2. `.claude/skills/requ-assign-packages/skill.md`
   - 4-signal heuristic (release_chunk → matrix lookup → semantic → sibling)
   - Prerequisite guard for unformalized flow chunks
   - Per-requirement proposal with reasoning
   - Accept all / Review individually / Skip flow
   - Writes accepted assignments + recomputes top-level target_package
   - Final pipeline call: `sync_task_packages.py --apply`

### Files Edited

3. `.claude/skills/release-plan/skill.md`
   - Action 4: inserted demo/boundary/size quality guidance
   - Step 2 menu: added item 6
   - Step 3: added Action 6 block (invokes requ-assign-packages, returns to Step 2)

4. `.claude/skills/requ-derive-from-flow/skill.md`
   - Renamed "Suggested Release Chunk" → "Suggested Package" in Notes block
   - Inserted Suggested Package column rules (chunking principles) before Summary self-check

5. `.claude/skills/INDEX.md`
   - Added `requ-assign-packages` to Quick Reference table
   - Added `requ-assign-packages` to requ-* skill category table

### Verification

Gap scanner output: `45 requirements, 285 unassigned items.`
(Goal.md expected ~41; actual 45 because some partial-assignment requirements were also found.
Plan_review.md had noted the count "may be stale" and should be treated as approximate.)

All 5 deliverables complete. All acceptance criteria in goal.md satisfied.
