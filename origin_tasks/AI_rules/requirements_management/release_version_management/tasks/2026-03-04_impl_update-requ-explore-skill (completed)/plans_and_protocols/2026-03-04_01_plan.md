# Plan: Update requ-explore Skill — Release Assignment Logic

## Task
TASK-PROC-034-05: Add release version assignment to requ-explore skill per-item during Phase 2 synthesis.

## Scope
- Modify `.claude/skills/requ-explore/skill.md` only
- Add new step 2.4 "Release Assignment" (shift current 2.4 Quality Check to 2.5)
- Keep text concise (token-sensitive)

## Implementation Strategy

### Step 1: Understand Current Structure
- Phase 2 = Synthesis & Writing (lines 99–336)
- Current steps: 2.1 (Determine Scope), 2.2 (Choose Approach), 2.3 (Document Structure), 2.4 (Quality Check)
- New step 2.4 will insert between 2.3 and current 2.4
- Renumber current 2.4 to 2.5

### Step 2: Write New Step 2.4
Insert after line ~282 (end of 2.3 Requirement Document Structure, before Quality Check)

Content should:
1. Explain when release assignment runs (after drafting, before quality check)
2. Reference RELEASES.md reading
3. Describe per-item prompt logic
4. Define trackable items = ACs or sections
5. Explain top-level computation (earliest version)
6. Handle skipping: internal process tooling with no release
7. Handle existing requirements: preserve assigned, prompt unassigned only
8. Reference REQ-PROC-034 SEC-04 table for behavior rules
9. Keep inline, concise (no lengthy narratives)

### Step 3: Update Quality Check (now 2.5)
- Shift section number from 2.4 to 2.5
- Add checklist item for release assignment verification

## Key Details

### Behavior Table (from REQ-PROC-034 SEC-04)
- New requirement with trackable items → Ask per item
- Existing requirement, item already has release → Preserve
- Existing requirement, item has no release → Ask
- RELEASES.md not found → Warn user, skip

### Semver Ordering Example
- 0.0.1 < 0.0.4 < 0.1.0 < 0.4.0 < 1.0.0
- Top-level = earliest among assigned

## Implementation Effort
- Single file edit
- ~20-30 lines of new text
- Minimal risk (text-only, no code changes)

## Quality Gate
- No Dart code (skill file, not lib/)
- Token efficiency check (no over-explanation)
- Accuracy: semver ordering, per-item logic, skip conditions
