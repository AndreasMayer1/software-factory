# Protocol: Update Skills and Scripts for Package-Based Model

**Task**: TASK-PROC-034-11
**Started**: 2026-03-26

---

## 2026-03-26
**Agent**: architecture-advisor (Sonnet)
**Agent ID**: unknown — session started without prior agent ID; find via `find /home/vscode/.ccs /home/vscode/.claude -name "agent-*.jsonl" 2>/dev/null | xargs ls -lt 2>/dev/null | head -20`
**Action**: Read goal.md, all 7 existing skill files (requ-explore, task-create, task-create-impl, requ-prep-release, ux-create-flow, requ-derive-from-flow, release), all 4 scripts (validate_meta.py, generate_status_overview.py, next_tasks.py, generate_technical_release_notes.py), RELEASE_BACKLOG.md (does not exist yet), and INDEX.md. Created implementation plan.
**Outcome**: Pass — plan saved to `plans_and_protocols/2026-03-26_01_plan_package-model-migration.md`
**Next Step**: Review plan and approve before implementation. Then implement in sub-task order: A (skills: requ-explore, task-create, task-create-impl) → B (new release-plan skill) → C (ux-create-flow, requ-derive-from-flow) → D (4 scripts) → E (requ-prep-release, release). Sub-tasks A, B, C, D are independent; E requires D.

---

## 2026-03-26 — Sub-task A
**Agent**: implementation-engineer (Sonnet)
**Agent ID**: a1756baa36917b800
**Action**: Implemented Sub-task A — migrated `target_release` → `target_package` / `RELEASES.md` → `RELEASE_BACKLOG.md` in 3 skill files.
**Files modified**:
- `.claude/skills/requ-explore/skill.md` — replaced section 2.4 (Release Assignment → Package Assignment) and updated 2.5 quality checklist item
- `.claude/skills/task-create/skill.md` — replaced "Release Version Inheritance" section with "Package Inheritance"; updated example snippet in template
- `.claude/skills/task-create-impl/skill.md` — replaced section 3.4 (same pattern)
**Outcome**: Pass — all 6 targeted changes applied. No other content modified.
**Next Step**: Sub-tasks B, C, D can proceed independently. E must follow D.

---

## 2026-03-26 — Sub-task B
**Agent**: implementation-engineer (Sonnet)
**Agent ID**: a9ecfbe8d7f7f7599
**Action**: Implemented Sub-task B — created `.claude/skills/release-plan/skill.md` with full workflow (Steps 1–4) from plan. Updated `.claude/skills/INDEX.md`: added `release-plan` to release section table and to quick reference table.
**Files modified**:
- `.claude/skills/release-plan/skill.md` — new file created
- `.claude/skills/INDEX.md` — 2 entries added (quick reference table + release section table)
**Outcome**: Pass — skill created and indexed correctly.
**Next Step**: Sub-tasks C and D can proceed independently.

---

## 2026-03-26 — Sub-task D3+D4 (verification and implementation)
**Agent**: implementation-engineer (Sonnet)
**Agent ID**: ab3134f63c5d51bcb
**Action**: Verified D3 (next_tasks.py) and implemented D4 (generate_technical_release_notes.py).
**Findings**:
- `scripts/next_tasks.py` (D3): All D3 changes already applied by a prior agent. Verified: `target_package` in `load_tasks()`, `RELEASE_BACKLOG_FILE` constant, `load_backlog_packages()`, `find_next_package()` with WHY comment, `rank_tasks_by_package()`, `_requirements_in_progress_by_package()`, `--package` CLI flag, and dual-mode ranking in `main()`. No changes required.
- `scripts/generate_technical_release_notes.py` (D4): Changes were missing. Applied all three plan changes.
**Files modified**:
- `scripts/generate_technical_release_notes.py`:
  - Added `RELEASE_BACKLOG_MD` constant
  - Added `find_active_package()` function — reads RELEASE_BACKLOG.md, returns package id with status: active; returns None gracefully if file missing
  - Added `find_version_for_package()` helper — resolves package ID to release version for output path
  - Updated `load_release_tasks()` to accept `by_package: bool = False` parameter with WHY comment about transition-period precedence
  - Updated `main()` with `--package` CLI flag and fallback logic: package first (RELEASE_BACKLOG.md), then release (RELEASES.md); warns when RELEASE_BACKLOG.md missing
  - Output path: `releases/[version]/` when using package ID (version resolved from RELEASE_BACKLOG.md), fallback to `releases/[package_id]/` if version not found
**Outcome**: Pass — syntax verified with `python3 -m py_compile`. All D3+D4 requirements met.
**Next Step**: All sub-tasks (A, B, C, D, E) are complete. Task TASK-PROC-034-11 can be closed.

---

## 2026-03-26 — Sub-task E (verification)
**Agent**: implementation-engineer (Sonnet)
**Agent ID**: a2e4399836653bce7
**Action**: Verified Sub-task E — compared current state of both skill files against plan sections E1 and E2.
**Findings**:
- `.claude/skills/requ-prep-release/skill.md`: All E1 changes already applied. Phase 0 has dual-mode (package vs. release) with correct `--package` / `--release` script flags and RELEASE_BACKLOG.md vs. RELEASES.md reads. Phase 6 has both branches (by-package: updates RELEASE_BACKLOG.md with active-conflict warning; by-release legacy: updates RELEASES.md).
- `.claude/skills/release/skill.md`: All E2 changes already applied. Step 4.1 reads RELEASE_BACKLOG.md first (active package), falls back to RELEASES.md, stops with error if neither found. Step 5 handles both paths (RELEASE_BACKLOG.md primary + RELEASES.md sync; RELEASES.md-only for legacy fallback).
**Files modified**: None — no changes required.
**Outcome**: Pass — Sub-task E fully complete as verified.
**Next Step**: All sub-tasks (A, B, C, D, E) are complete. Task TASK-PROC-034-11 can be closed.

---

## 2026-03-26 — Sub-task D1+D2 gap fix
**Agent**: implementation-engineer (Sonnet)
**Agent ID**: adeb7536c68c233b8
**Action**: Re-verified D1 and D2 against plan. Found 2 missing changes in `validate_meta.py`. Applied both. Confirmed all D2 changes already fully applied in `generate_status_overview.py`.
**Findings**:
- `scripts/validate_meta.py` D1: `known_packages` field, `_load_packages()`, `_validate_target_package()`, requirement-level dual validation, and `TaskMeta.target_package` field were all present. However `validate_tasks()` was NOT calling `_validate_target_package()` (only validated `target_release`), and was NOT passing `target_package` to `TaskMeta()` constructor.
- `scripts/generate_status_overview.py` D2: All changes already applied — `target_package` on both dataclasses, extraction in `_task_from_frontmatter()` and `_requirement_from_frontmatter()`, `load_backlog_packages()` function, `--package` filter in `main()`, `--package-summary` in arg parser and dispatch, `PackageSummaryReportGenerator` class, updated "Unassigned" heading and docstring.
**Files modified**:
- `scripts/validate_meta.py`:
  - `validate_tasks()`: added `target_package` validation (lines 507–510) with comment noting transition period
  - `validate_tasks()`: added `target_package=...` argument to `TaskMeta()` constructor (line 526)
**Outcome**: Pass — both scripts run without errors. `python3 scripts/validate_meta.py --verbose` completes; `python3 scripts/generate_status_overview.py --package-summary` completes.
**Next Step**: All sub-tasks fully complete. Task TASK-PROC-034-11 can be closed.
