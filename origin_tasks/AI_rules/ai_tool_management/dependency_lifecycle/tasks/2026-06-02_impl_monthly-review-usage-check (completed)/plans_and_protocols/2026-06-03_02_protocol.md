---
skills_used:
  - claude-automated-mode
  - claude-route
  - task-resolve
  - claude-write-script
  - doc-update-guidelines
  - task-complete
  - claude-commit
---

# Protocol: TASK-PROC-061-06 Monthly Review Usage-Check

**Date**: 2026-06-03
**Session**: e94f3c46-fc95-4e91-8fda-31cd98b93c06
**Agent**: Main session (claude-sonnet-4-6)

## Actions Taken

### 1. Explored existing structure
- Read `scripts/release/check_dependency_sweep.py` — existing advisory sweep script (Tier C pattern)
- Read `pubspec.yaml` — 38 direct deps + 18 dev deps; 4 SDK packages excluded
- Read `automation/dependency_reviews/2026-06/proposal.md` — shows current proposal format (no removal-candidates section)
- Read `doc/process/dependency_lifecycle.md` — policy doc referenced by the monthly review routine
- Read `requirements_tasks/.../requirements.md` AC-11 — full classification spec
- Read `doc/python/` guidelines (README, anti_patterns, architecture) — Tier C rules, type hints

### 2. Created plan
`plans_and_protocols/2026-06-03_01_plan.md`

### 3. Implemented `scripts/release/check_dependency_usage.py`
**Algorithm**: parse pubspec.yaml with PyYAML → scan *.dart files in lib/test/integration_test/
for `import 'package:<name>/'` → classify each package → output table or JSON.

**INDIRECT_REQUIREMENTS dict** (7 entries): sqlite3_flutter_libs, path, build_runner,
drift_dev, freezed, injectable_generator, json_serializable.

**Gate fixes applied**:
- C420: `dict.fromkeys(packages, 0)` instead of dict comprehension
- G2: `# type: ignore[import-untyped]` on yaml import (existing pattern)
- RUF005: list unpacking in test file

**Tier**: C (one-shot CLI, no imported callers)

### 4. Created `scripts/tests/test_check_dependency_usage.py`
12 tests across: `_classify` (pure function), `_parse_pubspec_deps` (tmp pubspec),
`_scan_import_counts` (tmp dart files), `classify_dependencies` (integration),
`main()` (JSON and human-readable output paths).

Test file correctly SKIPPED when PyYAML not in test env (same as 9 other yaml-dependent tests
in the suite — `pytest.importorskip("yaml", ...)`).

### 5. Updated `doc/process/dependency_lifecycle.md`
- Updated "Monthly Cadence & Per-Release Sweep" bullet: run usage-check pass first, then DG1-DG4
- Added new section "Monthly Review Usage-Check Pass (AC-11)": classification table, script
  reference, indirect-requirement documentation rule, proposal-structure rule

### 6. Updated CLAUDE.md Section 11
Added row: `| Which pubspec packages are unused / removal candidates | scripts/release/check_dependency_usage.py |`

## Quality Gate Results

| Gate | Result | Notes |
|---|---|---|
| G1 lint | PASS | After fixing C420 (dict.fromkeys) + RUF005 (list unpack) |
| G2 type | PASS | After adding yaml type: ignore[import-untyped] |
| G3 tests | Pre-existing failure | `test_orchestrate.py::TestFollowUpQuestionSafetyNet` — unrelated; my test properly SKIPPED |
| G4 no-handrolled | PASS | |
| G5 print discipline | PASS | |

## Acceptance Criteria Status

- [x] Script classifies all direct pubspec.yaml dependencies into (a/b/c) — `check_dependency_usage.py`
- [x] Script scans lib/, test/, integration_test/ for import statements
- [x] Indirect requirements documented with named chain — INDIRECT_REQUIREMENTS dict
- [x] Output distinguishes removal candidates from upgrade candidates — separate section + JSON key
- [x] Monthly review automation instructions reference usage-check before DG1-DG4 — `doc/process/dependency_lifecycle.md`
- [x] Python quality gates pass — G1/G2/G4/G5 PASS; G3 pre-existing failure only

## Outcome

PASS — all task acceptance criteria met.
