---
skills_used:
  - claude-route
  - task-create
  - requ-explore
  - claude-log
  - task-complete
  - claude-commit
---

# Protocol — exploration outcome

**Task**: TASK-PROC-061-18 · **Date**: 2026-06-03 · **Skill**: requ-explore (Scenario E, extension)

## What was done

1. **Re-validated** the design findings (design doc 01) against current repo state — the
   removal-candidate list and config-referenced false positives are unchanged.
2. **Framed the two open decisions** to the developer via AskUserQuestion:
   - Trial-removal gating policy → **allow-with-residual-risk** (confirm on CI-covered
     platforms; record uncovered platforms as accepted residual risk).
   - Which items become ACs → **all three** (evidence-class broadening, durable keep
     registry, trial-removal model).
3. **Extended REQ-PROC-061** (`status: active` living document):
   - Added **AC-13** (evidence-class broadening: config-referenced + native-declared, tiered output),
     **AC-14** (durable retention registry), **AC-15** (empirical trial-removal, allow-with-residual-risk).
   - Extended the usage-check classification table; added native-declaration circular-evidence caveat.
   - Added a **### False-Positive Recovery** subsection.
   - Updated the LLM autonomy-boundary table and added three Common Pitfalls.
   - Bumped `updated:` to 2026-06-03.
4. **Wrote the ADR** `decisions/2026-06-03_trial-removal-recovery-model.md` (records the
   gating-policy alternatives and the accepted decision + residual-risk consequence).

## Resolution of the ten items

| Item | Outcome |
|---|---|
| #1 config-referenced, #3 native-aware | AC-13 + tooling |
| #2 tiered output | tooling (observable output of AC-13) |
| #4 matcher gaps, #5 hygiene checks, #6 ergonomics | tooling-only (impl tasks) |
| #7 recovery registry | AC-14 |
| #8 trial-removal model | AC-15 + ADR |
| #9 local_notifier review | review/decision task |
| #10 native-limitation doc | doc-update task (AC-10 sync of `doc/process/dependency_lifecycle.md`) |

## Decomposition

Per developer instruction, `task-derive-from-requ` is run next on REQ-PROC-061 to
generate impl tasks for AC-13/14/15 (script changes via `claude-write-script`), plus the
`doc/process/dependency_lifecycle.md` sync (#10) and the `local_notifier` review (#9).

## Remaining uncertainty

- Accepted residual risk: removals confirmed only on CI-covered platforms can break an
  uncovered platform (macOS/iOS/Linux-desktop/Android-device today). Recorded per removal,
  shrinks as CI lanes are added.
- Exact registry file location/schema (`automation/dependency_reviews/kept.yaml` proposed)
  is an implementation decision for the derived impl task.
