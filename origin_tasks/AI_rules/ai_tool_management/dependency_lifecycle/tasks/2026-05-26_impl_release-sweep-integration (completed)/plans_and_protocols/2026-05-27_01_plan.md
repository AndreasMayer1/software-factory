# Plan: Release Sweep Integration (TASK-PROC-061-03)

## Date: 2026-05-27

## Approach: Inline

## Context
- `osv-scanner` not installed in devcontainer → must install via `claude-install-os-tool`
- `dart pub audit` does not exist as a subcommand
- In-scope manifests: `pubspec.lock` (root Dart project), `requirements-dev.txt` (Python scripts/)
- No `package-lock.json` found (no npm manifests in scope)
- Authoritative doc: `doc/process/dependency_lifecycle.md` (created by TASK-PROC-061-01)

## Deliverables

1. `osv-scanner` installed and confirmed working in devcontainer
2. `scripts/release/check_dependency_sweep.py` — standalone sweep script:
   - Runs `osv-scanner` over pubspec.lock + requirements-dev.txt
   - Runs `flutter pub outdated` and captures output (informational)
   - Exits 1 if any advisory found; exits 0 otherwise
   - Outputs clear findings for developer review
3. `scripts/release/check_release_preconditions.py` — new "Check 4e: Dependency sweep" 
   added before the final summary section
4. `doc/process/dependency_lifecycle.md` — reference to the script/gate added under
   "Monthly Cadence & Per-Release Sweep" section

## Gate point
Between the existing checks in `check_release_preconditions.py` (added as last check
before the overall summary, since it's the "release candidate" gate).

## Phases
1. Install osv-scanner → confirm `which osv-scanner`
2. Create `scripts/release/check_dependency_sweep.py` via `claude-write-script`
3. Modify `check_release_preconditions.py` to call the sweep via `claude-write-script`
4. Update `doc/process/dependency_lifecycle.md`
5. Verify: run the sweep script manually
6. `task-complete`
