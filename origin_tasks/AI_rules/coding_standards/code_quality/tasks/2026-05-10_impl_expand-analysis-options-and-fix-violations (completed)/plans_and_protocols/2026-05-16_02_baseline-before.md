---
date: 2026-05-16
type: protocol
task: TASK-PROC-046-03
---

# Pre-switch baseline (analyzer = flutter_lints + DCM block in analysis_options.yaml)

`flutter analyze` (run 2026-05-16, runtime 427s):

```
17920 issues found.
```

The captured tail of the output (last 30 lines) shows 28 real Dart-analyzer errors —
all in test files (`test/widget/minimal_resolution_test.dart`,
`test/widget/features/.../responsive_questionnaire_list_test.dart`):

- `uri_does_not_exist` (3) — files reference packages not in `pubspec.yaml`
- `undefined_function` (12) — `expect`, `testWidgets`, `MaterialApp`, `isA`, `Locale`, `Builder`, `group`
- `undefined_identifier` (4) — `GlobalMaterialLocalizations`, `GlobalWidgetsLocalizations`, `GlobalCupertinoLocalizations`, `isNotNull`
- `non_constant_list_element` (5), `undefined_class` (2), `non_type_as_type_argument` (1), `creation_with_non_type` (1)

The remaining ~17 890 issues are not captured in the tail and are almost certainly
`info`-level DCM rule findings (`prefer-trailing-comma`, `no-magic-number`,
`member-ordering`, `newline-before-return`, …) generated by the currently-active
`dart_code_linter:` block. They will vanish when that block is removed.

**Decision**: do not invest further effort in cataloguing the pre-switch state. The
DCM-config baseline is a moving target (the package is being removed in Part A) and
the meaningful measurement is the post-switch VGA + bloc_lint + clean_architecture_kit
count, captured in `03_baseline-after-vga.md`.
