# Protocol: TASK-PROC-046-20

## Session: 557a82d0

## Deliverables completed

### 1. `scripts/quality/check_quality_gates.sh` — modified
- Added `--quick` flag (strips itself from ARGS before forwarding to gate scripts)
- Added G1 gate (flutter analyze) as inline section before the script loop
  - Pass condition: zero errors/warnings (infos OK per REQ-PROC-046 G1)
  - Fixed: captures output and counts `error`/`warning` lines specifically
- Added per-gate extra-args map (`GATE_EXTRA_ARGS`) for passing --baseline to AC-06
- Added AC-06 gate to GATES array → `check_ac06_error_handling.py --baseline ...`
- Added G3 gate (flutter test) as inline section after the loop (skipped with --quick)
- Updated header comment (15 gates)

### 2. `scripts/quality/check_ac06_error_handling.py` — created
- Checks bare `catch (` without `on Type` guard
- Checks `throw` of string/int/bool/null literals
- Supports `--baseline FILE` to suppress pre-existing violations
- Supports `--dump-baseline` to emit current violations in baseline format
- Accepts `--exclude-paths FILE` (consistent with other gate scripts)
- Python gates: G1 lint ✅ G2 type ✅ G3 (pre-existing failure, not this code) G4 ✅ G5 ✅

### 3. `scripts/quality/ac06_violations_baseline.txt` — generated
- 47 pre-existing bare-catch violations (info-level in flutter analyze)
- Suppressed by baseline; gate passes on current code
- Developers reduce baseline by fixing violations file-by-file

### 4. `scripts/tests/test_check_ac06_error_handling.py` — created
- 23 tests covering: generated-file exclusion, bare-catch detection, typed-catch pass,
  multiline on-guard pass, literal throws, comment exclusion, exclusions, baseline loading

### 5. Lib/ fixes (pre-existing analyzer errors; fixed to enable G1 gate)
- `lib/core/domain/entities/plan_evaluation_input.dart` — removed 4 duplicate Questionnaire imports (unused)
- `lib/core/domain/entities/questionnaire_plan_entities/choice.dart` — removed 6 duplicate Question/QuestionType imports (unused in code)
- `lib/core/design_system/atoms/grid_example.dart` — changed return types to `List<GridItem>` (was `List<Widget>`)

## Performance impact

| Mode | Flutter analyze | Gate scripts | Flutter test | Total |
|------|-----------------|-------------|--------------|-------|
| Full (default) | ~5s | ~10s | ~53s | ~68s |
| Quick (--quick) | ~5s | ~10s | skipped | ~15s |

Recommendation: pre-commit hook uses default (no --quick). Test suite is 1305 tests;
53s is acceptable as a per-commit gate. Developers can use --quick for fast local
iteration if needed, but CI should always run the full gate set.

## AC-06 baseline note

47 pre-existing bare-catch violations exist (info-level in very_good_analysis).
These are legitimately violating AC-06 and should be fixed in a follow-up pass.
The baseline file `scripts/quality/ac06_violations_baseline.txt` tracks them.
To fix: update each file's bare catches to use `on Type catch (e)`, then remove
the corresponding lines from the baseline file.

## Gate runner final state

All 15 gates PASS on the current codebase:
G1 (flutter-analyze), SP1-SP4, AC11, AC12, AC06, AC02, type-naming, arch-imports,
no-direct-styling, test-smells, folder-taxonomy, G3 (flutter-test)
