# Plan — TASK-PROC-046-06: Update CLAUDE.md with back-pressure protocol and gate set

## Decision: where to insert

CLAUDE.md sections §1, §2, §4, §5, §7, §11 are heavily cross-referenced across the
repo (grep `CLAUDE.md §<n>`). Renumbering them would invalidate dozens of plan and
goal references. We therefore add the new content as a **subsection inside §7
"Coding Standards (Summary)"** rather than as a new top-level section.

§7 already contains the thin "Quality:" subsection and the "Python gates
(REQ-PROC-051)" subsection. The new subsection extends this pattern and lives
between them so §7's flow reads: Framework → Architecture → Git → Bugfix →
**Quality Gates and Back-Pressure Protocol** → Python gates → Windows bridge →
… (unchanged downstream).

## Content checklist (from goal.md)

- [x] List every active gate from REQ-PROC-046 (G1–G8), REQ-PROC-002 (TQ1–TQ4),
  REQ-PROC-052 (SP1–SP6) with tool + pass condition for each.
- [x] Five-cycle revision protocol (run → fail → read errors → revise → re-run all
  → escalate after 5 cycles via `automation/pending_feedback/<TASK_ID>/question.md`).
- [x] Reference A40 cold-start threshold (TASK-PROC-046-02 calibrated 7 500 ms;
  methodology in `doc/testing/cold_start_measurement_methodology.md`).
- [x] Reference safety-critical paths doc (`doc/testing/critical_paths.md`,
  produced by TASK-PROC-046-04 → already exists).
- [x] Reference `verify-quality` skill (TASK-PROC-046-11) as canonical enforcer.
- [x] Update §4 step 4 ("Verify") to invoke `verify-quality` rather than
  ad-hoc checks — already present, just tighten wording.
- [x] Cross-reference REQ-PROC-046 / REQ-PROC-002 / REQ-PROC-052 explicitly so
  reader can trace CLAUDE.md → requirement → analyzer config / script.

## Out of scope (per goal.md)

- Implementing missing gates.
- Changing the protocol's substance.
- Editing other skills.

## Structure of the new subsection

```
**Quality Gates and Back-Pressure Protocol** (REQ-PROC-046 / REQ-PROC-002 / REQ-PROC-052):

Three-paragraph intro: gates, five-cycle bound, escalation.

Table 1: Code-quality gates (G1–G8) — Gate | Tool | Pass | Cadence
Table 2: Test-quality gates (TQ1–TQ4)
Table 3: Privacy/security gates (SP1–SP6)

Five-cycle protocol bullet list (concise; full text in REQ-PROC-046 §Back-Pressure Protocol).
Pointer to `verify-quality` skill.
Supporting docs: critical_paths.md, cold_start_measurement_methodology.md,
analysis_options.yaml, scripts/quality/.
```

## §4 step 4 tightening

Current text:
```
4. **Verify** (via `verify-quality` skill):
   - Check against `doc/` guidelines
   - Verify no forbidden imports (e.g., UI in Domain)
   - Verify tests exist
   - **Verify WHY comments** exist for non-trivial code
```

Tightened text (still references the skill, but invokes the gate set rather
than ad-hoc check list):
```
4. **Verify** (via `verify-quality` skill — see §7 "Quality Gates and
   Back-Pressure Protocol"):
   - Runs all per-change gates from REQ-PROC-046, REQ-PROC-002, REQ-PROC-052
   - Spawns `quality-checker` agent for `doc/`-judgment-level review (WHY
     comments, persona alignment, architectural fit)
   - On RED: increments cycle counter, halts task-complete / commit hooks
   - At cycle 5: writes `automation/pending_feedback/<TASK_ID>/question.md`
     and terminates the session for developer review
```

## Effort estimate

Single CLAUDE.md edit (one new subsection, one §4 step rewrite). No code change.
No new scripts. Less than ~80 lines added.
