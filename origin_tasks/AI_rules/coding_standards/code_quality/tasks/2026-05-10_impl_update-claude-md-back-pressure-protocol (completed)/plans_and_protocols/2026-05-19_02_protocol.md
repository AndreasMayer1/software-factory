# Protocol — TASK-PROC-046-06

Session: a8c96a91-c639-48e0-be67-6f15d40cfaf2 (web account, automated)
Date: 2026-05-19

## What was done

Single edit to `CLAUDE.md`:

1. **§4 step 4 (Verify) rewritten** (lines 163–167) — replaced the four ad-hoc
   "Check / Verify …" bullets with a description that names `verify-quality` as
   the single canonical enforcement mechanism, lists the per-change gate sets
   it runs (G1–G6 / TQ1, TQ3 / SP1–SP6), names the cycle-state file
   (`plans_and_protocols/cycle_state.json`), and spells out the cycle-5
   escalation path. The skill reference was already there; this tightens what
   it actually does.

2. **§7 new subsection "Quality Gates and Back-Pressure Protocol"** (lines
   274–335) — inserted between the existing "Bugfix conventions" and the
   existing "Python gates (REQ-PROC-051)" subsections. Content:
   - One-paragraph framing: gates are closed, binary, enforced via back-pressure,
     `verify-quality` is the canonical enforcer.
   - **Table 1: Code-quality gates G1–G8** with tool, pass condition, cadence.
     Each row names the actual script under `scripts/quality/`. G7 row
     references the calibrated 7 500 ms A40 threshold and the methodology doc.
     G8 row references the size-analysis archive path.
   - **Table 2: Test-quality gates TQ1–TQ4** (same shape).
   - **Table 3: Privacy / security gates SP1–SP6** (no cadence column — all
     SP gates are per-change).
   - **Five-cycle protocol** — 7 numbered points. Includes the shared-counter
     rule, the cycle-5 escalation file format, the explicit "never write into
     `answer.md`" guard, and the doc-update-guidelines step 7 for non-obvious
     fix patterns.
   - **Authority and divergence** paragraph — when analyzer config /
     scripts diverge from the requirement, the requirement wins.
   - **Supporting docs and scripts** bullet list.
   - **General operational notes** — folded the four bullets from the old
     "Quality:" subsection ("dart fix --apply", "all tests pass" implicit in
     G3, "WHY comments", Windows-↔-container `pub get`).

## What was preserved

- Section numbering §1–§11 unchanged (heavily cross-referenced from
  `.claude/skills/**`, `.claude/agents/**`, requirements_tasks/**; grep
  confirmed >40 hits for `CLAUDE.md §<n>`). New content lives **inside §7**.
- "Python gates (REQ-PROC-051)" subsection left untouched (separate gate
  family for `scripts/**/*.py`; out of this task's scope).
- The Python-gate G1–G5 naming intentionally re-uses the "G1, G2 …" prefix
  for Python tooling; we did not rename it. The visual collision with
  REQ-PROC-046 G1–G8 is pre-existing and out of scope for this task.

## Acceptance criteria from goal.md

- [x] CLAUDE.md has a section listing all active gates from REQ-PROC-046,
  REQ-PROC-002, REQ-PROC-052 with tool / pass-condition for each.
- [x] The five-cycle back-pressure protocol is documented in CLAUDE.md with
  the escalation behaviour.
- [x] The `verify-quality` skill (or equivalent) is aligned with the gate
  set — §4 step 4 now invokes the gate set explicitly; §7's subsection
  names `verify-quality` as the canonical enforcer.
- [x] Cross-references from CLAUDE.md to each requirement and to the
  supporting scripts (coverage check, bundle-size check, grep gates) are
  present — every requirement file is named, every script is named.
- [x] An LLM agent reading CLAUDE.md can determine, without reading the
  requirement files, which gates apply and how to run each — the three
  tables answer this.

## Out of scope (per goal.md), confirmed not touched

- No new gate implementation.
- Protocol substance unchanged (this task documents, doesn't negotiate).
- No skill files edited besides via reference (verify-quality / skill.md
  was only read).

## Quality gates relevance

This task changed `CLAUDE.md` only. No files under `lib/`, `test/`,
`integration_test/`, `scripts/**/*.py`, or `scripts/**/*.ps1`. Therefore:
- Dart gates (REQ-PROC-046 / REQ-PROC-002 / REQ-PROC-052): not applicable.
- Python gates (REQ-PROC-051): not applicable.
- `verify-quality` skill: not applicable per its own preflight (no
  `lib/test/integration_test/` files changed).

## Follow-up gaps (none blocking this task)

- TASK-PROC-046-02 (A40 calibration) and TASK-PROC-046-04 (critical-paths
  doc) were both prerequisites of folding their outputs into CLAUDE.md.
  Both are complete enough that we could reference real file paths:
  `doc/testing/cold_start_measurement_methodology.md` and
  `doc/testing/critical_paths.md` both exist. No "see X once available"
  pointers were needed.
