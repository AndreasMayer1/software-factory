No mcp servers. they have security issues. use the cli tool of context7 instead. we have a skill to install things in the container, no problem.

privacy: yes we need such instructions to strip data from queries.

"Stability: how often the public API surface changes per year. low = bedrock APIs (dart:core); high = active rewrite churn." => that's confusing "stability: low" means that the api does not change very frequently, that means it is quite stable. or in other words the stability is high...

doc-lookup skill name: the skill is only for looking up dependencies, not code of the project. I suggest to make that explicit and call it doc-lookup-dependencies or similar.

code-bugfix skill-tool access: yes, option a

D3 — Lookup-log retention across release boundaries: yes i follow your recommendation

D4 — Trigger thresholds (§6 tables): create a task that is blocked and waits for release 0.0.1 to ship. the goal of the task is to investigate how well the thresholds and the lookup in general works and create a report with recommendations.

D5 — Per-package allowlist for the "high-churn" classification: yes follow your recommendation

D6 — Lookup budget scaling on Opus: no scaling

D7 — lookup_log.jsonl commit-message inclusion: yes, makes the evaluation later easier. we mioght remove that once we know that the lookup works well to save tokens.

D8 — context7 privacy acceptability: yes option a, but create a script that routes all requests and validates (at least what can be validated, e.g. paths - think about what can be validated). 

D9 — context7 call-counting confirmation: confirmed via web dashboard — 2 requests consumed for the test pair (ctx7 library flutter + ctx7 docs ...). So it is 2 calls per lookup when the slug must be resolved, 1 call when the slug is already known. The dedup/caching in lookup_log.jsonl already handles this: first lookup per technology = 2 calls, all subsequent = 1 call. Budget bands in §8.2 do not need to be halved.

D9 side finding — docs quality: querying /websites/api_flutter_dev for "ListView.builder itemBuilder signature" returned C++/Java/iOS embedder results, not Dart widget docs. The doc-lookup skill will need to use a more targeted library ID or query for Dart-specific API surfaces. Candidate IDs from the resolve test: /flutter/website (general Flutter docs, 2894 snippets) and /websites/api_flutter_dev (official API reference, 29111 snippets). The latter has more snippets but poor topic relevance for generic queries — query phrasing will matter. Needs further calibration during impl.


note: ctx7 installed a skill too. it lives in the ccs folder.

continue:
1. update the requirmeent if needed (consult the requ explore skill to know if it is required, it contains information about what should be part of a requ)
2. create the implementation tasks (of course respecting my feedback). create only one task for each "tier": 5 tasks. additionally create a 6th task that has the goal to verify that everything is implemenbted correctly.