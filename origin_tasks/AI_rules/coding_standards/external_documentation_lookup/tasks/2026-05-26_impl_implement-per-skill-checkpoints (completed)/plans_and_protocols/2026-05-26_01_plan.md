# Plan — TASK-PROC-053-04: Wire doc-lookup-dependencies into code-producing chains

Date: 2026-05-26
Agent: 55d45ce6-3315-4efa-b3d1-0419b24b5fc3

## Design reference

Synthesis: `tasks/2026-05-21_explore_operationalize-doc-lookup-policy (completed)/plans_and_protocols/2026-05-26_03_synthesis_design.md`
- §3.1 Chain map (C1–C5)
- §4.7 Checkpoint placement per chain
- §3.2 Agent inventory
- User feedback: code-bugfix option A (grow Skill tool)

Skill name: `doc-lookup-dependencies` (TASK-PROC-053-03)

## Checkpoint placement per chain (from §4.7)

| Chain | File to modify | Where | Type |
|-------|---------------|-------|------|
| C1 code-simple | `.claude/skills/code-simple/SKILL.md` | Step 3 — before spawning implementation-engineer: touch log; note agent runs checkpoint | Orchestrator init |
| C2 code-complex | `.claude/skills/code-complex/SKILL.md` | Step 5b — note per-batch checkpoint runs inside implementation-engineer; plan step optionally pre-warms | Orchestrator note |
| C3 code-test | `.claude/skills/code-test/SKILL.md` | Step 3 — note test-engineer Phase 2 invokes checkpoint | Orchestrator note |
| C4+C5 code-bugfix | `.claude/skills/code-bugfix/SKILL.md` | Slim step 2 + worktree step 6 — inline invocation + add Skill tool to frontmatter | Inline checkpoint |
| Agent impl-eng | `.claude/agents/implementation-engineer.md` | Step 2 (Implement) — before first call into any new API surface | Per-agent invocation |
| Agent test-eng | `.claude/agents/test-engineer.md` | Phase 2 (Implementation) — before writing first test using non-trivial framework API | Per-agent invocation |

## Specific additions

### code-simple SKILL.md (step 3)
Before "Spawn one implementation-engineer agent", add:
- Initialize lookup_log.jsonl (via touch) in the task's plans_and_protocols/
- Note: implementation-engineer runs doc-lookup-dependencies per AC-07

### code-complex SKILL.md (step 5b)
In the implementation batches section, add:
- Note: each implementation-engineer batch runs doc-lookup-dependencies per AC-07
- Optional: architecture-advisor MAY pre-warm the log in step 2 for APIs the plan names

### code-test SKILL.md (step 3)
Add note: test-engineer Phase 2 runs doc-lookup-dependencies per AC-07

### code-bugfix SKILL.md
- Frontmatter: add `Skill` to tools list
- Slim step 2: add invoke doc-lookup-dependencies before applying fix
- Worktree step 6: add invoke doc-lookup-dependencies before executing plan

### implementation-engineer.md (step 2)
Add: before emitting first call into any new dependency API surface in this task,
invoke `doc-lookup-dependencies --technology <id> --api-surface <dotted.path> --pinned-version <from-pubspec.lock>`
The skill deduplicates via lookup_log.jsonl.

### test-engineer.md (Phase 2)
Add: before writing tests that use a non-trivial test-framework API for the first time in this task,
invoke `doc-lookup-dependencies --technology <test-package> --api-surface <api-path> --pinned-version <from-pubspec.lock>`

## Modification order
1. implementation-engineer.md (direct Edit — agent file)
2. test-engineer.md (direct Edit — agent file)
3. code-simple SKILL.md (claude-modify-skill)
4. code-complex SKILL.md (claude-modify-skill)
5. code-test SKILL.md (claude-modify-skill)
6. code-bugfix SKILL.md (claude-modify-skill)
