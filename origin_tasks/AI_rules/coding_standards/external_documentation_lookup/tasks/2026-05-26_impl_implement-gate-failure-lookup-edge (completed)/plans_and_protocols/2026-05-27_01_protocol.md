# Protocol — TASK-PROC-053-06

Date: 2026-05-27  
Task: TASK-PROC-053-06 — Gate-failure → lookup edge + CLAUDE.md budget framework  
Status: IN PROGRESS  
Agent: 5b43d644-46d0-4b04-b2b3-dd07b00ed545 (session account: web)

## Design Reference

Synthesis §7 and §8 in:
`requirements_tasks/process/AI_rules/coding_standards/external_documentation_lookup/tasks/2026-05-21_explore_operationalize-doc-lookup-policy (completed)/plans_and_protocols/2026-05-26_03_synthesis_design.md`

## Deliverables

1. `code-simple` SKILL.md step 5 — failure-category table + pre-fix doc-lookup step
2. `code-complex` SKILL.md step 6 — same
3. `code-test` SKILL.md step 5 — same
4. `CLAUDE.md` §7 Quality Gates section — doc-lookup budget bands + cap-driven escalation

## Failure-category classification table (synthesis §7.2)

| Failure category | Detection | `--trigger` |
|---|---|---|
| Deprecation | "deprecated" / "deprecated_member_use" in analyzer output | `deprecation` |
| Unknown symbol | "Undefined name" / "isn't defined for the type" / `non_existent_method` | `unknown_symbol` |
| Signature mismatch | "argument type" / "isn't a subtype" / `extra_positional_arguments` | `gate_failure_api_mismatch` |
| Test framework subtle | test-runner stack-trace + matcher mismatch on async/golden/semantics | `test_framework_subtle` |
| SP gate (REQ-PROC-052) | SP1–SP6 violation on security-relevant package | `gate_failure_api_mismatch` |

## Budget bands (synthesis §8.2)

| Task effort | Max `looked_up` entries |
|---|---|
| XS / S | ≤ 5 |
| M | ≤ 10 |
| L / XL | ≤ 25 |

Enforced by `doc-lookup-dependencies` step 4. When it returns `BUDGET_CAPPED`, orchestrator routes to `pending_feedback`.

No Opus-tier multiplier (D6).

## Execution log

### 2026-05-27 — Inline execution

- [x] Read synthesis §7 + §8 for exact wording
- [x] Read code-simple, code-complex, code-test SKILL.md files
- [x] Read CLAUDE.md (full)
- [x] Read doc-lookup-dependencies SKILL.md (to confirm --trigger API)
- [x] Modify code-simple SKILL.md — gate-failure table + pre-fix lookup step added at step 5
- [x] Modify code-complex SKILL.md — gate-failure table + pre-fix lookup step added at step 6
- [x] Modify code-test SKILL.md — gate-failure table + pre-fix lookup step added at step 5
- [x] Modify CLAUDE.md — Doc-Lookup Budget block added to §7 (budget bands + cap-driven escalation + D6 no-Opus note)
- [x] Verify ACs — all three ACs satisfied; verify-quality untouched

## Result

COMPLETE. All deliverables implemented inline. INDEX.md and factory_flows.md: no sync needed (descriptions/diagram unchanged).

---

## 2026-05-27T(session end)
**Agent**: Main orchestrator (task-resolve / claude-log)  
**Agent ID**: 5b43d644-46d0-4b04-b2b3-dd07b00ed545  
**Action**: Completed TASK-PROC-053-06 — gate-failure → lookup edge wired into code-simple (step 5), code-complex (step 6), code-test (step 5); CLAUDE.md §7 updated with Doc-Lookup Budget block  
**Outcome**: PASS — All 3 ACs satisfied; verify-quality untouched; 4 files modified  
**Next Step**: task-complete skill to mark done and commit
