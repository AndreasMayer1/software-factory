# Protocol — TASK-PROC-051-02

## 2026-05-17 (planning phase)
**Agent**: architecture-advisor (Python tooling mechanism)
**Agent ID**: architecture-advisor-2026-05-17-unknown (session-spawn ID not recoverable from this context; resume via task path)
**Action**: Read goal.md, requirements.md (REQ-PROC-051), CLAUDE.md §7 & §11, scripts/quality/{check_quality_gates.sh,_lib.sh,README.md,check_no_telemetry_sdks.py,check_critical_path_coverage.py}, scripts/util/* listing (2/5 files used), scripts/automation/orchestrate.py (first 200 lines + grep for hand-rolled YAML pattern), and confirmed hand-rolled YAML parser pattern in 4 sites: orchestrate.py (≥4 occurrences), generate_status_overview.py, generate_id_registry.py, reconcile_dependencies.py. Reviewed claude-install-os-tool skill. Authored the design plan covering sections A–I.
**Outcome**: PASS. Plan persisted at `requirements_tasks/process/AI_rules/coding_standards/python_code_quality/tasks/2026-05-17_impl_python-tooling-config-and-gates/plans_and_protocols/2026-05-17_01_plan_tooling-mechanism.md`. Key decisions: ruff + mypy + pytest + ruamel.yaml; pyproject.toml + uv.lock + requirements-dev.txt fallback; `# tier: A|B|C` header-comment convention placed after module docstring; central helper at `scripts/util/yaml_frontmatter.py` exposing 3 entry points (read_frontmatter, update_frontmatter, frontmatter_session context manager); G4 + G5 as AST visitors at `scripts/quality/check_no_handrolled_yaml.py` and `check_print_discipline.py`; single runner `scripts/quality/check_python_gates.sh` structurally mirrors the Dart `check_quality_gates.sh`; CLAUDE.md §7 receives a Python-gates subsection. Implementation order: 16 files. 6 open questions raised for user (uv vs pip-tools, TIER C POC module choice, devcontainer install timing, missing-tier WARN-vs-FAIL behavior, `if __name__ == '__main__':` as CLI marker, mypy strict scope this task vs -04).
**Next Step**: User reviews plan + answers the 6 open questions. After approval, spawn implementation-engineer agent in BACKGROUND with 4:30 heartbeat (16-file impl + smoke run will exceed 5 min). Implementation must follow the file-order in §"Implementation order" and stop after the smoke run, leaving G4 intentionally failing on develop (the -04 cleanup task's job).
