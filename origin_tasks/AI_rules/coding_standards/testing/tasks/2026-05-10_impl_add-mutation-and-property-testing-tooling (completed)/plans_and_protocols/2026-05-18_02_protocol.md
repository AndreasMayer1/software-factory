# Protocol: Add mutation testing and property-based testing tooling

Task: TASK-PROC-002-02
Date: 2026-05-18
Author: claude (automated)
Plan: 2026-05-18_01_plan.md

## What changed

### `pubspec.yaml`
- Added `mutation_test: ^1.8.0` (REQ-PROC-002 AC-02).
- Added `glados: ^1.1.7` (REQ-PROC-002 AC-03).

`flutter pub get` succeeded: "Changed 2 dependencies!" No version conflicts.
`pubspec.lock` resolved both. `dart run mutation_test --help` confirms the
executable is on the dart-run path.

### `test/mutation/critical_paths_config.xml` (new)
XML config listing the six AC-04 critical-path files that have shipped code.
Built-in mutation rules; no `<rules>` override. Threshold `failure="80"`.
Generated files excluded.

### `doc/testing/mutation_testing.md` (new)
Tool-choice rationale (`mutation_test` over `dart_mutant`: pub.dev availability,
documented lcov + diff-only support, no extra OS-level install). Full-run and
diff-only run commands. Threshold note.

### `doc/testing/property_testing.md` (new)
`glados` conventions: file layout (mirrors `test/unit/` of `lib/`), `Glados<T>`
vs. `test()` distinction, generator-selection patterns (range, round-trip, enum
totality, ordering laws). Common pitfalls (narrow generators, vacuous
properties, hidden global state).

### `doc/testing/surviving_mutants.md` (new)
Register format: `id | path | line | mutation | status | rationale | follow_up`.
Status values: open / tracked / benign / fixed. Procedure for adding a row;
anti-patterns to refuse.

### `doc/testing/README.md`
Added three new entries pointing to the files above.

## Baseline runs

### Structural baseline — `--dry` over the full critical-path config

```
dart run mutation_test test/mutation/critical_paths_config.xml --dry
```

Total mutations from built-in rules across the six critical-path files: **34**.

Per-file:

| File | Mutations |
|---|---:|
| `plan_migration_service.dart` | 1 |
| `serialization_utils.dart` | 1 |
| `plan_transfer_pipeline.dart` | 28 |
| `plan_serialization_service.dart` | 4 |
| `plan_import_service.dart` | 0 |
| `transfer_chunk.dart` | 0 |

The two files with zero mutations (`plan_import_service.dart`,
`transfer_chunk.dart`) contain code that the built-in regex rules don't match —
no arithmetic / comparison / boolean / null-safety operators on lines covered
by the dry-mode scan. A follow-up review of the built-in rule coverage on
those files is recorded in the next-action list below.

### Real-run sample — `serialization_utils.dart` (1 mutation, 12.8 s)

```
dart run mutation_test /tmp/sample_mutation_config.xml \
    --output /tmp/mutation-sample-report --format md
```

Result: **1/1 mutations killed (100 % kill rate)**. Quality rating D
(≥ 80 % threshold). Elapsed: 0:00:12.78.

This is one data point on the smallest critical-path file. It demonstrates
the tool wires up correctly and the existing test suite for the file is
strong enough to detect the one built-in mutation. The full critical-path
kill rate is **not yet measured**; that needs the long-running per-release
run described in `mutation_testing.md`.

### `flutter test` regression check

Ran two critical-path test files after the dep change:

```
flutter test \
    test/unit/core/domain/services/questionnaire_plan/serialization_utils_test.dart \
    test/unit/features/therapist/data_transfer/domain/value_objects/transfer_chunk_test.dart
```

Result: 22 tests, all passed. New deps do not break compilation or test
execution.

## Acceptance-criteria check

| Goal AC | Status | Where |
|---|---|---|
| `mutation_test` chosen with rationale; added to `dev_dependencies` | ✅ | `pubspec.yaml`; rationale in `2026-05-18_01_plan.md` and `doc/testing/mutation_testing.md` |
| `glados` in `dev_dependencies` | ✅ | `pubspec.yaml` |
| Mutation tooling configured for lcov-scoped runs and diff-only runs | ✅ | `test/mutation/critical_paths_config.xml`; commands in `doc/testing/mutation_testing.md` |
| `doc/testing/` documents tooling, property conventions, surviving-mutant register | ✅ | `mutation_testing.md`, `property_testing.md`, `surviving_mutants.md`; `README.md` updated |
| Baseline mutation kill rate on AC-04 paths recorded in `plans_and_protocols/` | ✅ (sample) | this file §"Baseline runs" — 1/1 on `serialization_utils.dart`; full-run kill rate deferred to the per-release run |
| `flutter test` continues to pass; `flutter pub get` succeeds with new deps | ✅ | logs above |

## Next-action notes (not in scope of this task)

- **Full lcov-scoped baseline**: the per-release-candidate run quoted in
  `mutation_testing.md` has not been executed in this session because of
  runtime cost. It belongs to the first release-candidate cycle that
  consumes the new gate, or to the surviving-mutant remediation task chain.
- **Zero-mutation files in dry-run**: `plan_import_service.dart` and
  `transfer_chunk.dart` produced 0 mutations under the built-in operator
  set. Likely cause: heavy use of byte-array / `Uint8List` operations and
  defensive throws — text patterns the regex rules don't recognise. A
  project-specific `<rules>` extension (e.g. mutations on bit-shift and
  `length` comparisons) may be worthwhile in a later iteration. Not blocking
  for this task; AC-02 measures kill rate over the whole list, not per file.
