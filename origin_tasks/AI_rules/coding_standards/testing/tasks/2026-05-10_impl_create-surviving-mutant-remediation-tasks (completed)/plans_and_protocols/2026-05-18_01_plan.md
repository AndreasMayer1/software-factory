# Plan: Create surviving-mutant remediation tasks

Task: TASK-PROC-002-06
Date: 2026-05-18
Parent: TASK-PROC-002-02 (provides baseline)

## Input state

TASK-PROC-002-02 delivered the mutation tooling and a partial baseline:

- **Dry-run** over all 6 critical-path files: 34 mutations total
  - `plan_migration_service.dart` — 1 mutation
  - `serialization_utils.dart` — 1 mutation
  - `plan_transfer_pipeline.dart` — 28 mutations
  - `plan_serialization_service.dart` — 4 mutations
  - `plan_import_service.dart` — 0 mutations (regex doesn't match)
  - `transfer_chunk.dart` — 0 mutations (regex doesn't match)
- **Real-run sample**: only `serialization_utils.dart` was executed — 1/1 killed
- Full per-release lcov-scoped run was **deferred** by TASK-PROC-002-02 to "the first release-candidate cycle or the surviving-mutant remediation task chain"

This task is that remediation chain, so the full baseline must be produced here before classification is possible. Acting on the partial dry-run data alone would mis-classify untested mutants.

## Strategy

1. **Produce full baseline**:
   - `flutter test --coverage` to seed `coverage/lcov.info`
   - `dart run mutation_test test/mutation/critical_paths_config.xml --coverage coverage/lcov.info --output build/mutation-report --format all`
2. **Classify survivors** from `build/mutation-report/mutation-report.md`:
   - Cluster by `(file, mutation category)`
   - Each cluster: benign (with rationale) OR remediation-needed (with target assertion gap named)
3. **Record outcomes**:
   - Benign mutants → append to `doc/testing/surviving_mutants.md` register
   - Remediation-needed clusters → invoke `task-create` once per cluster under the owning feature requirement
4. **Document**: `plans_and_protocols/created_tasks.md` lists every created task ID

## Cluster → owning requirement mapping

| File | Category | Owning REQ |
|---|---|---|
| `plan_migration_service.dart` | Version migration | REQ-FUNC-006 (encryption/migration) |
| `serialization_utils.dart` | Version migration | REQ-FUNC-006 |
| `plan_transfer_pipeline.dart` | Data transfer | REQ-FUNC-007 |
| `plan_serialization_service.dart` | Data transfer | REQ-FUNC-007 |
| `plan_import_service.dart` | Data transfer | REQ-FUNC-007 |
| `transfer_chunk.dart` | Data transfer | REQ-FUNC-007 |

(REQ-FUNC-015 storage is not present in current config.)

## No-survivor fallback

If full run reports 0 survivors on all 6 files: record fact in `created_tasks.md`, no tasks created, task complete. Goal AC explicitly allows this no-op path.

## Out of scope (per goal.md)

- Strengthening assertions (each created task does it)
- Lowering the 80 % threshold
- Coverage gap remediation (TASK-PROC-046-09)
