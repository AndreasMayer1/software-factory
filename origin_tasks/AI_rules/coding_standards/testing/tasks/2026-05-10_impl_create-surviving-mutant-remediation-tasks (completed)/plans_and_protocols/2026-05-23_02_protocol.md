# Protocol: Create surviving-mutant remediation tasks

Task: TASK-PROC-002-06
Date: 2026-05-23
Author: claude (automated, session 9eccf6e7-bd30-4b4a-a8b0-39a177877a6e)
Plan: 2026-05-18_01_plan.md
Blocker (resolved): 2026-05-23_blocker_test-suite-not-green.md

## Blocker resolution

The earlier blocker (develop test suite red, `-8`) was fixed by the developer
(stale `Unit`→`void` assertions in `choice_service_test.dart`). Per the
answered `pending_feedback` (Option A), proceeded with the baseline run on the
now-green suite (`flutter test` → `+1192 ~2`, exit 0).

## What was done

1. `flutter test --coverage` → `coverage/lcov.info` (exit 0).
2. Full lcov-scoped mutation run over `test/mutation/critical_paths_config.xml`
   (~35 min). Result: 34 mutations, 33 killed, **1 survivor**, 97 % kill rate,
   rating A. Full report under `build/mutation-report/`.
3. Classified the single survivor (plan_transfer_pipeline.dart:66, arithmetic
   `+`→`-` on the random `sequence_id` bound) as **remediation-needed** — it
   narrows the AC-04 "random per transfer session" uint16 range and is missed
   because tests only exercise the deterministic `sequenceIdOverride` path.
4. Filed remediation task **TASK-FUNC-007-03-05** (under feat_plan_serialization,
   covers AC-04, after `[TASK-PROC-002-02]`) via `task-create-code`.
5. Recorded register row **SM-001** (`tracked`) in `doc/testing/surviving_mutants.md`.
6. Recorded everything in `created_tasks.md`.

## Key technical note for the remediation task

A naive `expect(seqId, inInclusiveRange(0, 65535))` does NOT kill this mutant:
the mutant's range `[0, 65533]` is a subset of `[0, 65535]`. Killing it needs
an assertion on the *upper bound itself* (Random injection asserting
`nextInt(0xFFFF + 1)`, or a named-constant `0x10000` the test pins). This is
captured in the created task's goal.md.

## Acceptance-criteria check (this task, TASK-PROC-002-06)

| Goal AC | Status | Where |
|---|---|---|
| Every survivor cluster classified remediation-needed / benign | ✅ | created_tasks.md (1 cluster → remediation-needed) |
| Benign mutants recorded with rationale | ✅ (none) | no benign survivors |
| Remediation clusters have a scheduled task | ✅ | TASK-FUNC-007-03-05 |
| created_tasks.md lists every created task ID | ✅ | created_tasks.md |
| Each created task names file:line + category + weak assertion | ✅ | TASK-FUNC-007-03-05/goal.md |

## Artifacts

- Mutation report: `build/mutation-report/` (not committed — build artifact)
- Register: `doc/testing/surviving_mutants.md` (SM-001)
- Created task: `requirements_tasks/functional/shared/epic_data_transfer/feat_plan_serialization/tasks/2026-05-23_impl_kill-sequence-id-range-mutant/`
