# Protocol — TASK-PROC-002-04 (inventory value objects for AC-03 property tests)

## 2026-05-16

**Agent**: general-purpose (vo-inventory)
**Agent ID**: a1767be4c297bd0c6
**Session**: a99fa007-9d34-4bd3-a276-b0490ea3462e (account: web)

**Action**: Walked `lib/core/domain/entities/`, `lib/core/domain/failures/`, and `lib/features/*/domain/value_objects/`. Read 36 candidate types. Classified each against the six AC-03 invariant categories (bounded range, enum totality, string length/format, ordering, round-trip, algebraic laws). Cross-referenced `test/unit/` for existing `Glados<T>` property tests (none exist — confirmed by `grep -r "Glados\|glados" test/`). Estimated per-gap effort (S / M / L). Wrote `plans_and_protocols/value_objects_inventory.md`.

**Outcome**: Pass. All five acceptance criteria satisfied:
- Inventory table covers every in-scope domain type with category classifications.
- Existing test files cross-referenced per type; existing example tests preserved (property tests strengthen, not replace).
- Effort estimate per gap.
- Total effort summary at top: 8×S + 6×M + 3×L (~10–12 h focused work).
- Five types with implicit/undocumented invariants flagged separately (`TimeInterval`, `TimeOptions`, `Questionnaire`, `QuestionnairePlan`, `Question`).

Headline counts: 36 inspected → 17 qualifying (19 with two marginal cases `OwnershipContext`, `TrackingEntry`) → 5 require invariant codification before property tests.

**Recommendation**: Split backfill into three sub-tasks aligned with three coherent clusters — (1) bounded-numeric + simple round-trip, (2) enum totality (collapsible into one parameterised file), (3) aggregate algebraic laws (requires custom generators and depends on the implicit-invariant codification).

**Key findings worth flagging upstream**:
- `Questionnaire` / `QuestionnairePlan` have an `isEmpty` guard enforced in `fromJson` but commented out in `create` — a real construction-path invariant inconsistency.
- `Question.create` contains dead-code branches in per-`QuestionType` validation; the invariant matrix should be rewritten as an explicit table before a Glados property test enumerates it.
- `TransferChunk` round-trip (bytes ↔ base64url over QR) is the highest-value property in the inventory.
- v1 mirrors carry identical invariants (spot-checked `LikertOptions` `[2,10]` bound); no separate property tests needed until a future schema change.

**Next Step**: Complete this task via `task-complete`. Backfill follow-up work (cluster sub-tasks + invariant codification for the five flagged types) will be created in subsequent `task-create` invocations driven by the requirement.
