# Plan: TASK-PROC-001-11 — task-create-code AC-07 iterative-fix Opus escalation

Date: 2026-06-02
Approach: inline (single skill file edit, clear shape)

## Objective

Close REQ-PROC-001 AC-07 zero-coverage gap. Extend `task-create-code` SKILL.md so
that code tasks driving a `verify-quality` iteration loop on `lib/` get
`opus_recommended: true` UNLESS the changed file set is named in goal.md and ≤ 3
files. Splitting-first is the preferred and required first response; escalation only
when the loop is inherent.

## Where

`.claude/skills/task-create-code/SKILL.md`, Phase 3.3 step **g) opus_recommended**
(the Opus Recommendation Check). Modification done via `claude-modify-skill`.

## Changes

1. Add a new sub-step **g.1) AC-07 — iterative-fix loop escalation** after the
   existing signal table in step g). It:
   - Detects the iterative-fix loop (S4): scope includes lib/ (and/or test,
     integration_test) AND the chain exercises verify-quality.
   - Detects closed (≤ 3 named lib/ files) vs open (globs/patterns OR > 3 files) scope.
   - Closed → no AC-07 escalation.
   - Open / > 3 → splitting-first; only escalate to opus_recommended:true with the
     AC-07 reason when splitting is confirmed infeasible (loop inherent).
   - Forbids auto-escalation without a splitting-first step.
2. Add a row to the **Automated Mode** checkpoint table for the AC-07 splitting-first
   decision (automated behavior: log + prefer splitting; escalate only if inherent).

## Acceptance check (from goal.md)

- [ ] SKILL.md contains AC-07 check at Opus recommendation step, referencing REQ-PROC-001 AC-07.
- [ ] > 3 lib/ files OR open scope + verify-quality loop → splitting-first prompt
      (interactive) / log (automated) before any opus escalation.
- [ ] opus_recommended:true set with AC-07 reason only when splitting infeasible
      (loop inherent); no auto-escalation without splitting-first.
- [ ] ≤ 3 named lib/ files + verify-quality loop → stays opus_recommended:false.

## Out of scope

- Retroactive re-evaluation of existing tasks (TASK-PROC-001-12).
- Changing how verify-quality runs.
- task-create (non-code) skill.
