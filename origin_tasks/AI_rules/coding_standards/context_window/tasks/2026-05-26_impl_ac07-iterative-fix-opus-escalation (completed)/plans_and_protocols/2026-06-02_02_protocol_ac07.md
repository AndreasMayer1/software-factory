---
skills_used:
  - claude-automated-mode
  - claude-route
  - task-resolve
  - claude-modify-skill
  - claude-log
  - task-complete
  - claude-commit
---

# Protocol: TASK-PROC-001-11 — AC-07 iterative-fix Opus escalation

Date: 2026-06-02
Agent: main session (orchestrator), automated mode
Session: a0bb80b7-429d-4ed6-a134-4de1ad685509

## Work done

Edited `.claude/skills/task-create-code/SKILL.md` and its `contract.yaml` via
`claude-modify-skill` to close REQ-PROC-001 AC-07.

### SKILL.md
1. Added sub-step **g.1) AC-07 — iterative-fix-loop escalation** to Phase 3.3 step g
   (Opus Recommendation Check). Logic:
   - Detect S4 (iterative-fix loop): scope touches lib/ (and/or test/, integration_test/)
     AND chain exercises verify-quality (mandatory for code-simple/complex/test).
   - Detect closed (≤ 3 named lib/ files, concrete paths) vs open (globs/patterns OR > 3 files).
   - Closed → no AC-07 override.
   - Open / > 3 → splitting-first principle; interactive prompts split-vs-inherent, automated
     logs splitting-first + prefers ≤ 3-file children; `opus_recommended: true` with the AC-07
     reason set ONLY when the loop is inherent/indivisible.
   - Auto-escalation without a splitting-first step is explicitly forbidden.
2. Added an Automated Mode checkpoint-table row for the AC-07 gate.

### contract.yaml
- Added a `user_input_gates:` entry (Phase 3.3g.1, decision_kind: path-selection,
  required: conditional). Passed `check_skill_contracts.py` (task-create-code clean).

## Verification

- `python3 scripts/quality/check_skill_contracts.py` → task-create-code: no violations.
  (3 FAILs are pre-existing baseline issues in claude-watch-tool-reliability,
  claude-write-hook, ui-verify-flutter — unrelated to this task.)
- `render_user_input_gates.py` regenerated `.factory/overview/user_input_gates.md`;
  the new AC-07 gate appears (line 82). NOTE: `.factory/overview/` + the render scripts
  were already modified/untracked at session start (other in-flight work) — NOT committed
  by this task to avoid sweeping pre-existing changes.

## AC coverage (goal.md)

- [x] SKILL.md contains AC-07 check at Opus recommendation step, referencing REQ-PROC-001 AC-07.
- [x] > 3 lib/ files OR open scope + verify-quality loop → splitting-first prompt
      (interactive) / log (automated) before any opus escalation.
- [x] opus_recommended:true with AC-07 reason only when splitting infeasible (loop inherent);
      no auto-escalation without splitting-first.
- [x] ≤ 3 named lib/ files + verify-quality loop → stays opus_recommended:false (no override).

## doc-update-guidelines

N/A — change is to a process skill (`.claude/`), not lib/test/integration_test code, and
involved no non-obvious gate-failure workaround. doc/ holds coding guidelines only.

Automated: yes
