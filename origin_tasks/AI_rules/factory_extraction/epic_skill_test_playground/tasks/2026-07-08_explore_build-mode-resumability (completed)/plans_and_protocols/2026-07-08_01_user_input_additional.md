Note that the task that has the goal to run the derivation on the test harness app might also be executed with the autorun. That means that in the outer project there's also an orchestrator running. That means that the test harness app contains an auto run. So an orchestrator will be started there as well while the auto orchestrator is still running. If we want to pause the session that runs outside of the test harness, we also have to pause the outer orchestrator. Otherwise it would start another task. At least that is my current understanding. Please explore.

Another topic is that this stop or pause mechanism we are building now for the use case task derivation. It's also helpful to have that for the other use cases of the skill playground. For example, if we want to test a skill that is doing a lot of work and maybe also needs multiple tasks, then it would be nice if we could also use this mechanism we are building here to pause the auto tasks execution or the auto session.