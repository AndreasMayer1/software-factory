# Reset note — full from-scratch re-derivation (materialization-layer-aware)

Date: 2026-07-14 · interactive session · developer-directed

## Why reset

Developer directive: reset TASK-PROC-068-12 to run the **whole** middle-layer derivation
chain from scratch, to validate the corrected end-to-end mechanism — now that a new
**materialization layer** has landed upstream of the flow layer.

## What changed upstream since the 2026-07-10 blocker (03) that makes the old plan stale

1. **TASK-PROC-068-23 (build-resume fresh-session-id fix) is COMPLETE.** This was the #1
   blocker (decision A) in `2026-07-10_03_blocker_...` and in the pending_feedback question.
   Resume of a preserved build-mode run is now viable. Added 068-23 to `after:`.
2. **A new materialization layer landed** (REQ-PROC-074 authoring/enforcement/brownfield +
   REQ-PROC-075), sitting between Scenario and Flow. It is now a hard gate:
   - `ux-flow-draft` Pre-flight presence+provenance gate (074-02-02) — flow drafting fails
     closed without a `product_materialization.md` (MAT-NNN).
   - `layer-derivation-start` / `layer-derivation-resume` materialization precondition on
     every invocation (074-03-04); brownfield + MISSING → "detected-provisional: confirm the
     user-facing set" (does not fail closed; may reverse-derive materialization). In automated
     mode this writes a `pending_feedback` question and stops.
   - Flows carry bidirectional `authored_against_materialization <-> dependent_flows`.
   - The harness has **no** materialization artifact — it must be reverse-derived (Path 1,
     developer-chosen) as part of this run.

## Reset actions performed

- Deleted DRAFT middle-layer artifacts from `test_harness_app/`:
  - `user_flows/detailed_entry_after_movie/flow.md` (FLOW-001)
  - `user_flows/quick_rating_after_movie/flow.md` (FLOW-002)
  - `user_flows/FLOW_INDEX.md`
  - `requirements_tasks/functional/flow_layer/requirements.md` (REQ-HARNESS-01 bucket) + tasks/.gitkeep
- **Kept** personas + scenarios (anchors) — they are the brownfield corpus the
  reverse-derivation reads to confirm the user-facing set.
- `goal.md`: `status: in_progress -> pending`; cleared `session_id`/`session_account` pins;
  added `TASK-PROC-068-23` to `after:`.
- Removed `automation/pending_feedback/TASK-PROC-068-12/` (stale — decision A resolved, B/C
  overtaken by the from-scratch re-derivation).
- Verified `.playground_runs` registry is empty — no stale build-mode run to re-attach to;
  the next run starts fresh.

## Known open item to resolve before the run (NOT part of the reset)

- The derivation spec's `fixed_layers` predates the materialization layer. Confirm whether
  the materialization layer must be added to the chain spec, or whether the precondition's
  brownfield reverse-derive handles it out-of-band, before launching the build-mode run.

## Expected behaviour of the from-scratch run (Path 1 — reverse-derive in-run)

The layer-derivation precondition will find materialization MISSING + brownfield corpus
present -> "detected-provisional" -> escalate via `pending_feedback` (automated) and stop.
build.py detects the inner question (`automation/pending_feedback/*/question.md`), classifies
the run BLOCKED, preserves the deployed copy, and harvests nothing. Developer answers the
question inside the preserved copy; `playground-build-resume` (now unblocked by 068-23)
relaunches. This exercises the answer->resume round-trip end-to-end — the genuine unknown.
