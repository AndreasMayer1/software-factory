# Task Reset — TASK-PROC-068-12 (2026-07-08)

## What changed in goal.md frontmatter
- `status: in_progress → pending`
- `session_id` / `session_account` **cleared** — forces a **fresh** session on next dispatch. The prior
  session escalated (see `2026-07-08_01_blocker_spec-and-enrichment-seam.md`) and a `--resume` would
  continue its context, which is polluted with the wrong cross-project framing. A fresh session re-reads
  the (to-be-corrected) goal.md instead.
- `awaiting` set (interim permanent hold) — see `awaiting_note`.
- `after` gained `TASK-PROC-071-06-07` (the enrich/dispatch-seam fix, B2 — completed).

## Why the `after:` does NOT point at TASK-PROC-068-20
068-20 is an **explore** task. It completes when the resumability mechanism is *designed* and the impl
tasks that *build* it are **created** — not when they are **built**. Setting `after: [TASK-PROC-068-20]`
would unblock 068-12 the moment the explore finished, i.e. **before** the mechanism-building impl tasks
run — reintroducing the failure. The impl tasks that 068-12 truly depends on **do not exist yet**, so
they cannot be listed in `after:` today.

## The wiring that unblocks this task (owned by TASK-PROC-068-20)
When 068-20 creates the resumability impl tasks (via `task-derive-from-requ`), it must:
1. Populate this task's `after:` with **those impl-task IDs** (the tasks that build the mechanism).
2. Re-author this task's **"How to Approach"** to the build-mode / deployed-copy path (run the derivation
   *inside* the isolated deployed copy as its own project via `build.py run_build_mode`, then harvest —
   **not** `layer-derivation-start` cross-project from the host).
3. Keep `session_id` empty (fresh session).
4. Clear this `awaiting` hold.

The stale pending question was archived to `automation/answered_feedback/TASK-PROC-068-12/` (with a
`RESOLUTION_NOTE.md`) so the orchestrator no longer attempts to resume the old session.
