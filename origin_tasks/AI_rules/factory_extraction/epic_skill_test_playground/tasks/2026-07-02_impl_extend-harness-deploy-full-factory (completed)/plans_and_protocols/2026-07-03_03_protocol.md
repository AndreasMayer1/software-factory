---
skills_used:
  - claude-automated-mode
  - task-start
  - claude-route
  - task-resolve
  - claude-write-script
  - claude-log
  - verify-quality
  - task-complete
  - claude-commit
---

## 2026-07-03 (session 49f95366-4629-4938-81b9-2b2675477080, account gmail2)

**Agent**: main session (automated mode, no sub-agent delegation — task was
small enough to stay inline per CLAUDE.md §2 economics)
**Agent ID**: main session 49f95366-4629-4938-81b9-2b2675477080
**Action**:
- Wrote plan (`2026-07-03_01_plan_whole-factory-deploy.md`) covering the
  resolved exclude sets, entangled-tree sub-folder handling, and a discovered
  git-reset topology hazard in the real `test_harness_app/` (not its own git
  repo — `git reset --hard` invoked with `cwd=test_harness_app` would reset
  the ENTIRE outer repo, empirically verified in a throwaway scratch repo).
- Extended `scripts/playground/deploy.py::deploy_candidate` (via
  `claude-write-script`) to copy the whole host factory project into
  `harness_dir` via a TEMPORARY exclude-based `shutil.copytree(...,
  dirs_exist_ok=True)` — top-level excludes + `requirements_tasks/{functional,
  non-functional}` sub-folder excludes. `dirs_exist_ok=True` (no upfront
  `rmtree`) preserves the harness's own `.git` across repeat deploys.
- Updated `scripts/tests/test_playground_deploy.py`: replaced the two tests
  whose expected behavior changed (stale-skill removal is now
  `reset_harness()`'s job, not deploy's; the FileNotFoundError check is now
  about `host_project_dir` itself, not `.claude/skills/`) and added 5 new
  tests for the whole-factory-copy + exclude-rule behavior.
- Ran `scripts/quality/check_python_gates.sh`: G1/G2/G4/G5/G6/G7 PASS; G3's 2
  failures (`test_aggregate_read_metrics.py`) confirmed pre-existing on
  baseline via `git stash` + re-run — not a regression from this change.
  `test_playground_deploy.py` + `test_playground_run_skeleton.py`: 28/28 pass.
- Executed the real AC-10 functional proof against a disposable scratch git
  repo (never the real `test_harness_app/`, per the discovered hazard):
  real `deploy_candidate` → real `wrap_with_containment` (bwrap available) →
  ran `scripts/artifacts/doc_governance.py --list-violations` (the
  `doc-split` skill's underlying script; anchors on
  `Path(__file__).parent.parent.parent`; stdlib-only — the goal's suggested
  `generate_id_registry.py` needs `ruamel.yaml` from a path bwrap doesn't
  bind, an environment constraint unrelated to deploy correctness) →
  succeeded (rc 0) using only deployed content → negative control confirmed
  a host-tree sentinel is unreachable from inside the jail while reachable
  outside it → bonus: exercised `reset_harness()` on the scratch repo,
  confirming the deploy→run→reset cycle works end-to-end and that the reset
  stayed scoped to the scratch repo only. Evidence written to
  `2026-07-03_02_evidence_ac10-functional-proof.md`; scratch dir + sentinel
  deleted after the run, no residue.
- Marked goal.md AC-10 checkbox done, pointing at the evidence file.

**Outcome**: PASS. AC-10 satisfied with real (non-mocked) evidence. No
regressions introduced (confirmed against develop baseline). `doc-split`
skill unaffected (only additive read of its underlying script for the proof).

**Next Step**: `task-complete` (marks task done, updates requirements,
regenerates status overview, commits). No further tasks spawned by this
task — the discovered git-reset topology hazard in the real
`test_harness_app/` is documented in the plan for the developer's awareness,
not fixed here (restructuring `test_harness_app/`'s git topology is a
separate, much larger change touching AC-01/AC-06/AC-11 assumptions,
outside "extend deploy.py (and reset/launch as needed)" scope).
