2026-04-29: Updated `.claude/skills/claude-autorun/skill.md` (Phase 4 of TASK-PROC-041-01-09) — replaced all `automation/.stop-requested` sentinel file references with `state.json["stop_requested"]` writes in three locations: `stop` action (full rewrite to python3 atomic JSON update), `status` action bash check, and monitoring cron prompt; `grep -n "stop-requested" skill.md` returned 0 hits confirming complete removal.
