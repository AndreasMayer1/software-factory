## feedback

once the new skll process is implemented, how do we migrate the currently existing tasks? release-begin-impl was called for release 0.0.1, task-derive-from-requ completed for that release. there are even one or two coding tasks already started. additionally we can't be sure that enough scribble tasks exist for 0.0.1. delete all tasks and start release-begin-impl again? costs a lot of tokens. 

should we do flutter_app/requirements_tasks/process/AI_rules/factory_extraction/tasks/2026-05-28_explore_software-factory-extraction/goal.md first (which is a huge effort) but it would also allow us to develop the factory skills on a much better level and also would allow us to create a mechanism that allows us to test them. i would suggest to create a simple angualar/react web app for example a personal movie rating app (only offline, just a place for an individual user to take notes for his personal movie or book ratings) that is the testing playground. less complicated and much less token costs per skill workflow run.

L3 Seam owner is depth-1; the source check does not recurse. => is it safe to forbid recursion? (also L5)

release-begin-impl (Wave 1) creates coding tasks for pure-domain units (no scribble). would it make the creation of the scribbles easier if the domain entities already exist on scribble creation? imagine a complex form that asks the user for many data points, all with different format and validation. creating the ui for that will be much easier with a complete and precise definition of those data points...

encapsulation: are our phases defined by the skills encapsulated (enough)? and with single responsibilities? but of course it still holds: token efficiency, that's more important, but we need to make the trade off decisions explicit in the requ that describe the skills.

D-2: per-design-unit

D-3: names approved; task-start wrapper over claude-route: i already created a separate task for that - consider it done.



## your tasks

write a synthesis/answer document based on the feedback above, then:

I want you to back pressure on every aspect systematically. but in 2 abstraction levls: first think about each topic as a whole: what speaks against the rationale for it? how to do it differently? how to improve it? 
then do the same for each chapter in each topic.
write a report for each topic as file.

create another document that synthesizes again: i want a plan that recommends next steps