---
skill: task-derive-from-requ
mode: automated
decision: ""
task_id: TASK-PROC-032-30
captured_at: 2026-06-06
---

# Question

---
task_id: TASK-PROC-032-30
session_id: dc27d645-874a-4cd6-b6ac-8852c1dabf72
account: gmail
status: awaiting_answer
asked_at: 2026-06-05T19:20:00Z
skill: task-derive-from-requ
---

# Pending Question (follow-up)

You approved the next step "run `task-derive-from-requ` on REQ-PROC-032". I started it and hit two
things that need your decision before I create any tasks.

## ⚠ Primary decision — sequencing: derive the consistency slice **now**, or **defer**?

New information from the redesign manifest (`…/2026-06-04_explore_redesign-implementation-workflow-scribble-gate (completed)/plans_and_protocols/2026-06-05_13_implementation-task-manifest.md`):

- The manifest marks the T-C consistency tasks (**T-C8…C14, C17**) as **`[DERIVED]` — "generated by
  `task-derive-from-requ` *after A lands*"** (lines 13, 119, 126). "A" = the whole authoring stage
  **T-A1…T-A5**, fanned out in parallel after T-A1.
- A-stage is **not** fully landed. The sibling A-stage authoring tasks are still `in_progress`:
  - **TASK-PROC-032-31** — T-A3, generator carrier-format & **review-layer** (PROP-1/3/4/13C)
  - **TASK-PROC-032-32** — auto-review control model
  - **TASK-PROC-032-33** — T-A5, flow-viewer (PROP-14)
- The consistency tasks **interleave** with those: e.g. **T-C11** wires `ui-scribble-cross-feature-checker`
  + `ui-scribble-auto-review`, and **T-C12** wires `ui-scribble-generator` + reviewers — components whose
  ACs are authored by T-A3 / the auto-review task, not yet in REQ-PROC-032.

**Consequence of deriving now:** the consistency-layer tasks would be created with dependencies on ACs
that don't exist yet → dangling cross-task `after:` links, likely rework once the siblings land. This
contradicts the manifest's own "after A lands" sequencing.

**Options:**
1. **(Recommended) Defer derivation.** Close TASK-PROC-032-30 now — its deliverable (AC-42…AC-55 authored
   into REQ-PROC-032) is complete and verified. Instead of running `task-derive-from-requ` here, I create
   one follow-up **derivation task** gated `after:` the sibling A-stage authoring tasks (032-31/32/33),
   so the full T-C set (consistency + generator/review-layer + flow-viewer) is derived together once A
   lands. This becomes the recorded "next step" for AC-6.
2. **Proceed now anyway.** I derive only the consistency slice (AC-42…AC-55) immediately, accept the
   dangling deps, and rely on `task-repair-meta` to reconcile cross-task dependencies after the siblings
   land. (If you pick this, also answer the cross-ref classification below.)

**Please answer: 1 (defer) or 2 (proceed now).**

## Secondary — cross-reference classification (only acted on if you pick Option 2, except REQ-PROC-035)

REQ-PROC-032 currently has `after: []` and does not reference these. Full candidate list with my
recommendations is in `…/plans_and_protocols/2026-06-05_cross_ref_gaps.md`. Classify each as
`hard` (→ `after:`), `semantic` (→ Related Requirements), or `ignore`:

| REQ-ID | Recommended |
|--------|-------------|
| REQ-PROC-035 (two-wave model; SCI edges depend on it) | **hard** |
| REQ-PROC-058 (`--scope {presentation,code}` consumes facet-tags) | **semantic** |
| REQ-PROC-069 (same redesign program) | **semantic** |
| REQ-PROC-030-01 (requirements-pipeline consistency) | **semantic** |
| all other detector hits (generic "consistency"/"scribble") | **ignore** |

> Note: adding **REQ-PROC-035 to `after:`** is correct regardless of the sequencing decision, since the
> new SCI ACs reference its two-wave model. If you pick Option 1, I'll apply just that one cross-ref when
> closing, and leave the rest for the future derivation task.

## What I need from you
1. **Sequencing: Option 1 (defer) or Option 2 (proceed now)?**
2. If Option 2: confirm/adjust the cross-ref classifications above.

# Developer Answer

<!-- AWAITING_HUMAN_ANSWER -->

⚠️  AUTOMATED SESSIONS: Do NOT write to this file.
This file is reserved for human responses only.
Writing here as an automated session violates the safety protocol.

The developer will open this file and type their answer below — replacing or appending to this text.


---
## ⚠ STRUCTURE CHANGE — developer-authorized (2026-06-06)

REQ-PROC-032 was restructured into an EPIC + 7 child FEATURES (zero specification change; all AC/section
text migrated byte-exact and independently verified). The ACs you authored now live in feature **REQ-PROC-032-05** (`feat_consistency_sci_layer/`). Your authored ACs AC-42..55 → AC-01..14.

CONTINUE: run `task-derive-from-requ` on `feat_consistency_sci_layer/requirements.md` — a clean 1:1 with
your consistency slice. The original 'defer vs proceed' blocker is RESOLVED: the A-stage authoring
(carrier, auto-review, flow-viewer) has landed as sibling features, so the ACs those tasks reference now exist.
NOTE: your original defer/proceed question (above) still awaits the developer's explicit answer.
CONTINUE against the FEATURE, not the epic (the epic is non-implementable).

DEVELOPER DIRECTIVE: every task you create via task-derive-from-requ MUST be appended to
`.claude/task_ordering_priority_override.txt` (process tasks carry no target_package and won't
surface in next_tasks.py otherwise).

Crosswalk + restructure record:
requirements_tasks/process/AI_rules/workflows/ui_sketch_iteration_workflow/tasks/2026-06-06_impl_restructure-req-proc-032-into-epic/plans_and_protocols/

# Rationale Captured

(Automated archival — no rationale extracted.)
