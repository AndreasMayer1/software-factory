I approve everything unless I state otherwise below.

Regarding the new agents. We have to be careful. First I want to say yes, let's create new agents. Good idea. Then I have to say we have to be careful how we name them and how we actually write them. Currently we don't have a skill to write new agents. We only have a skill to write new skills. With the skills in the past we noticed that we need a naming scheme because there are a lot of skills and for the agents I think we need the same. So before we create new agents, we actually have to think about how to name them. But maybe we can just use the same naming scheme we use for skills. I don't know. And then maybe we have to Yes uh find out what is the best phrasing or what first the best phrasing and second the best content in agents. I mean we also already discovered that uh those words that are used in this han plug-in do really provide a benefit. So for example that I want to also apply here. Also the new skill needs to set the allowed tools, for example. So we need a heuristic or some way to determine which rules must be allowed for this agent.

I really like the Reviewer pre-brief.
We need to define how the content of this pre-brief document actually looks like what content must be included and what content must not be included and how verbose it should be because we don't want to overload the user with too much information early on. Because I can imagine that the user will do iterate on this pre-brief. So the workflow must also already define this iteration process at this stage.

The persona embodiment is also a very good idea. You said you would cap it to three personas. I think this uh number should be flexible. Maybe uh we let the LLM decide how many personas and which personas make the most sense. Because for some user flows they are very similar. And for other user flows they are completely different. So fixed number might not be the right choice here. And we have to specify how the process is like um is the LLM forced to read only the persona file? Probably not. We also have uh sc scenarios, but those scenarios often are only pre-product scenarios. So they describe a world without the app. That's very helpful if you want to emphas um empathize to understand what the user needs are. I think we have to try out if it is needed that the LLM actually reads those as well. Or only the persona and the user flow documents.
Do you think it also makes sense to create an agent just for that? It's a very clear scope, right? Remembers me that it would be nice to have a strict rule or button. Maybe strict is the wrong word, but to have uh rules or heuristics when we want to create agents and when not. Maybe the new agent creation skill we need um actually contains this so that it's always easy to determine when a new agent should be created and when not. I think that agent creation skill is quite big and out of scope of it. Maybe you can create maybe you can spawn an agent that actually has the goal to uh create the first draft of such an agent skill. And maybe that um is also first uh doing a web search to check if uh there are some recommendations about how to write agents, when to use agents, stuff like that, so that we can incorporate that already in the first version of the new skill.
Back to the persona embodiment review: I wonder if Sonnet is strong enough enough to do that. But I think we have to save tokens and I would actually do the reviews with Sonette first. And once we've tried everything out. And we are not satisfied with the results, with the quality, we can still uh change the scribble workflow and allow opus as well. 

Also the cross feature consistency check. Very nice.

As well as the flow navigation as log structural commitment. We still have somewhere this flow scribble index, right? If I remember correctly in the past I noticed that hey, if every feature creates its own scribbles, um how do we make sure that it's possible to um find the scribbles that relate to one flow? But um Yeah if the scribbles are distributed in several features. So I think we do have an index somewhere with all the scribbles relating to one flow, right? Maybe that's also where we could place this flow navigation. But of course it must be then findable or the LM that later implements it has to know where to look for it.

I also like the fatigue detection.

Visual-validation Must use Opus. So net is not strong enough to actually do good visual validation...


Okay, I think we do have to think the script scope through. If we use position C the h hybrid approach. And the coder actually implements something and the user makes a review of the flutter app and says, Oh, I don't like the wording here, for example. Then the coder is actually allowed to choose a different wording. He has to document it. What we now have is a discrepancy between the scribbles and the actual implementation. The question is, is that a problem? We have to think longer into the future. What happens if we make user research and discover oh this user flow has to be uh changed a little bit. So we actually update the user flow and then we update the requirements and then we create new scribbles. What happens to all the changes that were made only in the Flutter app? Will the documentation that is created by the coding agent be good enough to recreate the decisions that were made in the new scribbles? I think it's a risk. Look from another perspective. You could say that code is disposable and the real source of truth must always be the requirements. The goal being that even if the c code is completely deleted, uh Agent could rewrite everything from scratch based on the requirements and the result would be the same. Well not the individual coding lines but the behavior of the app would be the same. That is much more robust, but on the other side some things like we already know cannot really define in the scribble. But the more we define we do not define in the scribble, the more likely it is that we have a drift at one point, that the implementation results actually vary from code change to code change because um not everything is defined in the requirements. Okay. Telling you that I actually would prefer position A.


I think we already have a huge work to do now. But that's fine. We don't have to actually execute all implementation tasks we will create. Um but I think it makes sense now that we are brainstorming this to actually do create all the implementation tasks so that in the future we can then implement it. That said. I actually want to do another brainstorming round, Just because you had so good ideas. Um and I think you stopped because um it was already enough, right? But um that's why I want to ask you again if you could do another ideation round.

And then I do have a question regarding requirements splitting. For example, now we have a future a regarding data transfer. If we uh say everything related to the user interface that is relevant for the data transfer is inside this feature, we are actually fine. The scribble skill will read the feature and then understand what should be on the user interface and scribble it. Now imagine that the security feature, for example, um it's the encryption of the file that is created for transfer. And imagine that all the user interface parts that are relevant for this encryption are not defined in the transfer feature, but only in the security feature. In this scenario, I think we can agree that the scripples belong to the transfer feature. Because that's actually what the user wants to use. That's the main use case for the user The question of course is who decides what the main use case for the user is? How is that determined? And how robust is it? Of course someone could argue that it should be more generic because um not all aspects of the UI are defined by only one feature, so it should be outside of those features. That's the first thing I want to mention. The actual location where the scripples are placed. And then the second thing, and that's I think the most or the more relevant part is is the scribble skill able to find all relevant requirements to create everything needed in the user interface. Maybe that's already uh solved. I just worked uh on the reorganization of features uh and requirements, folder structure. That's why I'm now thinking about this topic here. Uh to me it's currently not clear how to make that fully transparent. Just one last thought. Maybe it's better and that's just an idea, right? An idea for evaluation. Maybe it's better to place the scribbles not below individual features or epics, but create user flow scribbles. We decided not to do the scripts for user flows. It's complicated. Because a user flow is like a flow through the app, right? And on each screen the user has different interaction possibilities. And one user flow could, for example, only use one interaction possibility on this screen, and all other interaction possibilities belong to other flows. That means that the user flow does not define how a screen must look. It just defines a part on only the relevant part of the screen. As stated above, the same holds true for the requirements. They are also only focused on one aspect. They are focused on a different aspect than the user flows. So the clustering or the perspective is different, but it still slices the app into parts. And the screens, the scribbles actually combine everything. Should we spend a bit more time to think about that reality and how we want to solve it so that it is transparent, explicit, and easy to understand and to work with?