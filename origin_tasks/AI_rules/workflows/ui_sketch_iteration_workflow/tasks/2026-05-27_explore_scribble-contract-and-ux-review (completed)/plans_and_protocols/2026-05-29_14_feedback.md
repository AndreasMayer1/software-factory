4.4. 
.claude/schemas/<artifact>.yaml
Just note. Most artifacts already have a definition. Usually in the root folder where they are stored, for example, all the markdown files and the requirements. User needs folder describe how the artifacts look like. important note: when the refactoring/migration happens we have to clean up carefully: no duplicate stale documents must remain.

revision_request file
Should we uh make this more general? So that there is the penning feedback folder for questions. And then there is the revision request folder. Where it's more about reviews. We have other places and other skills that also create reviews. Currently they just use the penning feedback mechanism and ask if the creation if the created suggestions are good enough, basically for the question is if the proposals are good and even if it's like an iteration, an iterative workflow, um they still use the penning feedback folder. If we do it like that, we might have uh interference with the orchestrator because currently the orchestrator of course only uses the penning feedback and it's of course possible that skills that will write in the revision request are also executed with the orchestrator. At the same time we also have improvement suggestions in the factory folder.

"Defer to backlog": where does this backlog live? remember: we need tasks to do work - tasks are the backlog.

requirements_matrix.md may not exist for every flow: see the requ-derive-from-flow skill.

TASK-PROC-057-01 (apex) running in parallel: NEW-EXPLORATION's executor reads TASK-PROC-057-01 progress at start. do it

"5. Agent vs session distinction (clarifying)"ff from 2026-05-29_13_session_token_efficiency_analysis.md Contains relevant information for the agent creation skill. Because that skill shall also evaluate if an agent makes sense for the desired use case.

The token efficiency analysis comes to the conclusion that some parts should be actually done by agents. My question is. Do we need all of the sub skills? If a subsk only spawns an agent, maybe it's better to write a description what the subskill does in the agent and Replace the subskill by this agent.