regarding the skill workflow, skill order and information flow back: I miss the integration into the mechanism that actually allows the llm to do any work: tasks. only if a new task is created, work will be done a handoff file/log alone is a dead end. i for example noted that the requ-explore skill also sometimes terminates without follow up and created: 
- flutter_app/requirements_tasks/process/AI_rules/factory_quality/requirements.md
- flutter_app/requirements_tasks/process/AI_rules/factory_purpose/tasks/2026-05-26_explore_factory-purpose-and-improvement-loop/goal.md
- and probably other requ and exploration tasks.
we need to align all the efforts. avoid duplicate work. make sure every task knows of the other tasks and pieces so that a holistic view emerghes when executing. the tasks must await each other so that the order makes sense.
we need to make a decision: the current project state is that we started the implementation of release 0.0.1. it's the first time we do this. we noticed some things that need to be improved befgore continuing with the coding work. therefore we used flutter_app/.claude/task_ordering_priority_override.txt to do work before the coding for 0.0.1 continues. one of the things is the scribble skill. i'm not satisfied, so i started this exploration that we're currently working on. the scribble skill needs to be improved before work on 0.0.1 can go on. therefore: all tasks that are required for that must be added to flutter_app/.claude/task_ordering_priority_override.txt. we seem to have dependencies now on the overall skill workflow improvements that are already planned (some tasks created). i did not add them to flutter_app/.claude/task_ordering_priority_override.txt cause i thought that they can wait for 0.0.1 to finish. the question now: can they wait? efficiency is king. it doesn't matter how long it tsakes before we continue to code 0.0.1. i want to minimize the total amount of work. if the skill workflow improvemnts are in total less work to do now, we should do it.

4. Bidirectional feedback patterns
I want to ask the question. Should we Focus the skills or the artifacts? You wrote it like with the focus on the skills. But for example, if you consider Prince 2 They decided to put the artifacts into focus for a good reason, right? but i might be wrong, evaluate.
revision_requests/ or create a task? whats better?

5.2 Flow navigation
The user needs uh instructions how he has to do the validations. That contains the correct screen order and what he has to or what is relevant on that screen for the flow. Same as the auto revived extension. But for the human. Maybe yes, single sentence as instructions would be enough to tell him what file he has to open.

"requirements.md frontmatter: add presentation_layer: true/false (whether the requirement has UI scope) — many already have related fields; this just standardizes" => Did you check what fields currently exist? I don't want to introduce a new field that duplicates what's already there.

"flow.md frontmatter: add serves_requirements: [REQ-...] listing requirements that contribute UI to this flow's screens (this enables reverse lookup; requ-derive-from-flow populates it)" => requirements_user_needs\README_8_CROSS-REFERENCING_SYSTEMS.md and requirements_user_needs\user_flows\_clusters\flexible_data_transfer\requirements_matrix.md

"Update the relevant skills: requ-explore, ux-create-flow, requ-derive-from-flow — to emit these fields" => Don't just blindly add new fields. Like I said above. We have to check what's already there and integrate.Maybe we can just use what's there.

6.7 clarification — auto-review brief vs inter-version diff: approved

7. Divide-and-conquer — sub-skill split for the scribble pipeline
Note that the skills are just text files. They are not executed like or programm would be. That means that in theory the LLM could just read all of the skills and would then possess all information needed. But of course that's not what I want. I want to like you already did reflect the workflow in the existence of the skills. I want to do that to increase maintainability and so on. So that each component, each skill can be understood on its own. 
We also have to think about how that integrates into our tasks. We need to think about how many sessions are needed. That includes how many agents are then spawned by this session. Is it still only one session? Or are there multiple tasks created for each stage? And every task then is executed in a new session. Or that to evaluate have to consider that every new session starts with empty context. That means it has to read all files again, which is expensive. So just starting new sessions or agents comes with a cost. But of course on the other hand the same applies if you just use the same session for multiple things that are not related and there is information in the context that is not needed anymore. It makes sense to start a fresh session because um e each token in the context costs money. So I suggest to do a separate analysis for the question What information is needed at which stage? Which split makes sense to minimize overall token consumption: only relevant information in the context + As few readings of documents as possible. Both goals interfere with each other... I think this investigation deserves a own document like we did for the redundancy check.

Scribble-location migration policy is light. => For that to work, we really have to also improve how features in the lib folders are structured and when new features are created and what is in one feature. I mean we have similar problems everywhere like when how to structure the requirements for example or how to structure the guidelines in doc/. There are already requirements and exploration tasks that can be used for inspiration. But of course on implementation level, I mean at dart code level, there are probably other rules to follow.

In general for the gaps you've identified. We have to fill them, of course. We need to create additional tasks. If needed.Of course those additional tasks might belong to a different requirement and not the scribble requ.

The exploration agent you started that created that exploration task. Client that did a web research, but that was not deep enough and there should be a not a round of investigation. Also, I think it did not understand everything completely and should read more of the code, but of course that's then part of the exploration task that must be created. I just want to highlight that the information we have now from this agent you started is incomplete and we should not depend too much on it. And we should be opening that this exploration task that will be created for this topic will bring information that changes our approach in some ways. Maybe we should actually write this task first, execute it, and then continue here to iterate.That means we should not create all tasks you propose already, but only this single one. And then continue our iteration. With the results.

The proposed sub-skill split is one specific cut.  NEW-EXPLORATION should validate before SCRIBBLE-SPLIT executes. => yes.

Persona-embodiment-reviewer reading scope grows. => yes, The user flows are already created. With all scenarios in mind. Therefore I think reading just one scenario is enough. Because as I said, it must be the most relevant one.

The 11-bundle plan is ambitious. => Yes, we can think about what to execute now and what to execute later, but as I already said above. I'm interested in the total efficiency, the total cost. Things that we don't refactor now might lead to more work when we then actually refactor it because other things get added in the old style and those new things then also have to be refactored.

## redundancy-check document

Option B: create a separate claude-modify-agent skill. Symmetric with claude-create-agent. Cleanest design. => yes

approve all redundancy-check decisions