Recover or consciously kill the lost strand: i want the complete plan implemented. we didn't do the work to now ditch it.

Fix the claude-modify-agent / claude-create-agent references: better would be to actually  create those skills before exe4cuting the scribble work. start an agent to check if we already have a task for that or at least a requ.

Home DOMAIN-VOCAB (D9) is actually something that a  good claude-modify-agent / claude-create-agent skill must provide. that will be not easy for a llm, because very advanced expert terms are needed and llms like to be shallow, but with the right instructions (and potentially web search - but such expert terms are also rare in the web, it's more book/research thing) it might work.

Widen the verify task (032-20): yes! make the verification as potent as possible. it's the final guard.

Whether the scribble-location migration and flow_navigation.yaml were intentionally dropped (a judgment that they're lower priority than the content contract) or accidentally lost: accidentally

please make a plan how to modify the tasks and what tasks to add additionally. also how to modify the requ.