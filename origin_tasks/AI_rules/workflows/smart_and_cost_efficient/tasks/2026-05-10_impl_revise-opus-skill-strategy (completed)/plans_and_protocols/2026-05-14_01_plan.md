# Plan: Eliminate Mid-Session Model Switching

Date: 2026-05-14
Task: TASK-PROC-031-03
Approach: Opus mode (session already on Opus 4.7); inline edits per skill/agent; per-phase commits.

## Phase order (per goal.md)

1. **A** — Agent default models + remove warnings → commit
2. **B** — CLAUDE.md "Model Selection Guide" → commit (user review)
3. **C** — `scripts/automation/orchestrate.py` pre-pick + `--model opus` → commit
4. **D** — `claude-route` opus_recommended warning; remove forwarding → commit (user review)
5. **E** — Strip Opus branches from skills (one commit per skill, low-risk first):
   1. claude-ask
   2. doc-update-guidelines
   3. task-resolve
   4. ux-write-persona, ux-write-scenario, ux-flow-draft
   5. claude-modify-ordering-rules
   6. requ-explore
   7. requ-derive-from-flow, requ-verify-flow-coverage
   8. task-create, task-create-code (remove Opus mode entirely)
   9. code-complex, code-test (agent-spawn changes)
6. **F** — Delete `.claude/skills/claude-switch-opus/` and `.claude/skills/claude-workflow-opus/` → commit (user go/no-go)
7. **G** — Memory cleanup; INDEX.md + factory_flows.md update → commit

## User review points
- After Phase B (Model Selection Guide wording)
- After Phase D (claude-route warning text)
- Before Phase F (go/no-go to delete skill folders)

## Rollback
Each phase is one or more commits. `git revert <commit>` restores prior phase.

## Concurrency note
Other tasks running in parallel may modify unrelated files. Every commit uses explicit `git add <path>` for files I touched only — never `git add -A`/`git add .`.

## Memory verification path
goal.md line 211 points at `instances/web/...` but the actual user memory path is
`/home/vscode/.ccs/instances/gmail2/projects/-workspaces-private-mood-tracker-flutter-app/memory/`.
Phase G uses the correct path.
