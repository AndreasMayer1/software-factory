All the attempts to test and improve the new testing workflow were made with the gpt-5-mini model. Azure gave a away free credits, so I spent it to test gpt 5 mini... 
This task is the 10th test of the testing workflow. It performed worse than other tasks before. It seems like that the model is not able to work like that as a agent with the given instructions. That does not mean that other models, e.g. Gemini are not able to follow the workflow successfully. In my experience Gemini works way better with roo code and my instructions. The experiment to improve the testing workflow with gpt stops here.

All changes made to the workflow are kept, since they might work for gemini or other models. But currently this is not tested yet. Howevery I switch to Claude Code for now which won't use this workflow anyways. Time will tell which workflow documentation approach is best - it's good to keep the progress for later reference of the learning experience.

Task is incomplete, but closed.