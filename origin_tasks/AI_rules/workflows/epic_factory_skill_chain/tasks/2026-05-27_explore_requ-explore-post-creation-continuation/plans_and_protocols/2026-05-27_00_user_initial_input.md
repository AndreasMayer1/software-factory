we must somehow prevent that the requ-explore skill ends with a created requirement and nothing more. that breaks the chain: a task to improve, tasks to implement the requ or a task to derive those implementation tasks are needed. the question is: what of those? ask the user? invent an heuristic/rules when to do what? we don't want to ask the user more questions than necessary. 
one thing to keep in mind: the session that created the requ already holds a lot of information and often enough information to just quickly creating the task that impl the requ without having to reread everything. Sometimes the session that created the requ might actually know more than a fresh session that can only reread the produced files and has no access to the thinking and reasoning the session made when creating the requ. on the other hand is a session with a huge context also very expensive to run... 

i notice: its more complex than i thought. please create a new exploration task for that.
