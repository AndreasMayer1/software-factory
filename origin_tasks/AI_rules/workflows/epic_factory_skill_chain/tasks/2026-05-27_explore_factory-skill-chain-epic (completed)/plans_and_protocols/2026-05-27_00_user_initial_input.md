i don't think we should create a dedicated requ just for that. it belongs to a requ that either describes that we want a good workflow through the skills or one that describes what the requ-explore skill does.

know what? I think we actually create a requirement that has the generic goal to have a perfect workflow. that's something which is currently documented in flutter_app/.claude/factory_flows.md
I think it helps to have requ to be able to explicitly work on that in the future. i see it in flutter_app/requirements_tasks/process/AI_rules/workflows. of course it overlaps with requ that cover single skills, so it must be referenced. maybe it should be an epic?
create an exploration task for that, then update the task you created before.
