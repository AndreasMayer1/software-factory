# Plan: Pilot One Self-Contained han Specialist Agent (Frozen Copy)

**Task:** TASK-PROC-055-04  **Date:** 2026-05-27

## Approach: Inline

Single session — fetch, adapt, create, modify skill, run pilot, record.

## Phases

1. **Fetch agent file** → fetch `adversarial-validator.md` from han GitHub; record source commit SHA
2. **Adapt & create agent** → rename to `han-adversarial-validator`; adapt formatting to our conventions; write to `.claude/agents/`; verify self-contained (no han skill deps)
3. **Create THIRD_PARTY_NOTICES.md** → repo root; MIT text + copyright + SHA + adapted file list (OR-3)
4. **Edit code-complex skill** → via `claude-modify-skill`; add optional adversarial-validator step after step 2 (Plan) for Medium/Large sizes; explicitly-invoked only
5. **Run pilot** → spawn `han-adversarial-validator` agent on a recent task's plan/protocol; record findings
6. **Record in protocol** → `plans_and_protocols/2026-05-27_protocol.md` with pilot outcome (OR-5 + OR-6)

## Agent Choice

`adversarial-validator` — synthesis report §3.2 marks it as top pilot candidate. It:
- Is self-contained (no han skill dependency)
- Fills a clear gap: we have no "disprove the plan/finding" role
- Uses sonnet (structured validation along defined paths)

## Naming Convention

`han-adversarial-validator` — prefix `han-` avoids collision with our existing agents and documents provenance.

## Deferred (YAGNI)

- **gap-analyzer**: second candidate; defer until adversarial-validator pilot confirms signal
- **Wiring into ui-create-scribble**: per goal.md, only if pilot shows measurable signal; defer to TASK-PROC-032-09
