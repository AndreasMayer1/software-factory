---
date: 2026-05-30
type: protocol
task_id: TASK-PROC-006-14
agent_id: main-session
---

# Protocol: TASK-PROC-006-14 Implementation

## Execution Summary

### Phase 1 — Background agent (a4379e9794db3a0bd)

Scope: `scripts/factory/aggregate_read_metrics.py` only.

**Completed:**
- Added `shutil` + `timedelta` imports, `AGGREGATOR_PRUNE_DAYS = 30` constant
- Added `prune_old_sessions(logs_root, prune_days)` per REQ-PROC-044 §6 Developer Guidelines:
  - Iterates session dirs, finds most-recent JSONL timestamp, removes dir if older than prune_days
  - Fail-safe: sessions with no parseable timestamps retained
  - Silent no-op when nothing to prune; keys on timestamp field (not mtime — per Common Pitfalls)
- Added `_load_existing_fingerprints(events_dir)` helper for deduplication
- Updated `emit_events()` to skip events whose fingerprint already exists in events_dir
- Updated `aggregate_logs()` signature to accept `prune_days` param; calls `prune_old_sessions()` first
- Added `--prune-days N` CLI flag (default: 30)
- Added 4 new tests: prune removes old, retains recent, retains no-timestamp (fail-safe), dedup skips duplicate fingerprint
- Gate result: G1-G5 PASS (1 pre-existing failure in test_orchestrate, not introduced)

### Phase 2 — Main session (inline)

#### Change 2: `scripts/optimize/select_candidate.py`

- Added `DIMENSION_TOKEN_COST = "token_cost"` constant
- Added `high_read_file` case in `classify()` before catch-all:
  - `"cache"` in `optimization_candidates` → `("optimization", DIMENSION_TOKEN_COST)`
  - otherwise → `("optimization", DIMENSION_CLARITY)`
- Added 3 parametrize entries to `test_classification_table_matches_skill_body`:
  - cache+section candidates → token_cost
  - section+reference candidates → clarity
  - empty candidates → clarity
- Gate result: G1-G5 PASS; 1020 passing (+3 new)

#### Change 3: `scripts/optimize/run_monitors.py`

- Added `import subprocess`
- Added `_AGGREGATOR_THRESHOLD = 5` constant
- Added `_invoke_aggregator()` function: calls `aggregate_read_metrics.py --emit-events` via subprocess (check=False, capture_output=True)
- Updated `run_all()`: after monitors loop, reads state.json; if `completions_since_last_run >= 5`, calls `_invoke_aggregator()` in boundary guard try/except
- Updated module docstring to reflect aggregator invocation
- Updated tests: added `mc` import + `_LOW_COMPLETIONS` sentinel; guarded all existing tests with `mc.load_state` mock to prevent unintended aggregator subprocess invocation in CI; updated `test_runs_under_two_seconds` to prevent aggregator latency skewing timing test
- Added `test_aggregator_invoked_when_threshold_met` and `test_aggregator_not_invoked_below_threshold`
- Gate result: G1-G5 PASS; 1022 passing (+2 new)

#### Change 4: `.claude/skills/claude-optimize/SKILL.md`

- Added `high_read_file` row to Step 2 classification table:
  `| high_read_file | optimization | — | token_cost (cache in candidates) / clarity (otherwise) |`
- Mirrors `select_candidate.py::classify()` — the single source of truth per the skill's own rule
- INDEX.md: no update (description unchanged)
- factory_flows.md: no diagram change (internal table row, not new input/artifact/edge)

## Acceptance Criteria Verification

- [x] `aggregate_read_metrics.py --emit-events` prunes sessions older than --prune-days (tested)
- [x] `emit_events()` skips duplicate fingerprints within a run (tested)
- [x] `run_monitors.py` invokes aggregator iff `completions_since_last_run >= 5` (tested)
- [x] `select_candidate.py` maps `high_read_file+cache` → token_cost (tested)
- [x] `select_candidate.py` maps `high_read_file` without cache → clarity (tested)
- [x] G-INV-2: aggregator is subprocess call from run_monitors.py, not an agent tool
- [x] SKILL.md Step 2 table has `high_read_file` row
- [x] All Python quality gates pass (G1-G5; pre-existing G3 failure is not our regression)

## Gate Results (final run)

G1 lint: PASS | G2 type: PASS | G3 tests: 1022 pass, 1 pre-existing fail | G4 no-handrolled: PASS | G5 print-discip: PASS
