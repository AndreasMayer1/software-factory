# Protocol — TASK-PROC-006-09 (IMPL-D)

## 2026-05-28

**Agent**: main session (automated mode, claude-opus-4-7)
**Agent ID**: 10a5ce79-6317-4648-8750-7386732755ae
**Action**: Implemented `scripts/optimize/create_optimize_task.py` per the plan
in `2026-05-28_01_plan.md`. The script is the single chokepoint for
claude-optimize improvement-task creation; G-INV-1 (`awaiting:
["user-unblock"]`) is a hard-coded literal at one site
(`_render_frontmatter`) with a code comment naming AC-04, and the SEC-04
deny-list is a module-level `DENY_LIST` tuple matched via `fnmatch` so the
`scripts/quality/**` glob catches nested files.

Companion tests at `scripts/tests/test_create_optimize_task.py` cover:
* G-INV-1 across 36 (target × dimension × research) flag combinations plus an
  explicit no-research path — all assert `awaiting == ["user-unblock"]`.
* One test per SEC-04 deny-list entry (8 entries; all rejected with exit code
  `EXIT_DENYLIST=2` and a clear "matched pattern: <X>" message).
* `scripts/quality/**` glob matches nested files.
* Normal target produces a valid goal.md (frontmatter parsed via the
  canonical `scripts/util/yaml_frontmatter.read_frontmatter` helper).
* optimization_approach with and without web-research (query omitted when
  not recommended).
* `source_event:` populated from a monitor event file; missing /
  malformed / partial event files return `EXIT_INVALID=3`.
* Invalid `--optimization-target` and `--optimization-dimension` rejected
  by argparse.

57 tests, all green (0.14s).

**Quality gates** (`scripts/quality/check_python_gates.sh`):
* G1 lint (ruff) — PASS
* G2 type (mypy) — PASS
* G3 tests (pytest) — PASS when `CLAUDE_SESSION_ID` is unset.
  An unfiltered run fails `test_orchestrate.py::TestBuildEnv::test_no_session_id_when_empty`
  because automated mode sets `CLAUDE_SESSION_ID` in the inherited env; the
  test is unrelated to this change and the same failure reproduces on develop
  without my edits.
* G4 no hand-rolled YAML — PASS
* G5 print discipline — PASS

**Outcome**: PASS. All AC bullets in goal.md are satisfied:
* Script exists at `scripts/optimize/create_optimize_task.py` with `--help`.
* Every produced goal.md has `awaiting: ["user-unblock"]` (verified
  exhaustively over flag combinations).
* No code path produces an unblocked task.
* Every SEC-04 path is rejected with a pattern-named error.
* `scripts/quality/**` glob rejection verified for a nested file.
* `optimization_target`, `optimization_dimension`, and
  `optimization_approach` block are emitted.
* The auto-block line carries a comment naming G-INV-1 and AC-04.

**CLAUDE.md Section 11**: no update — the script is only ever invoked by
the future claude-optimize skill (Rule 3 skills-only exclusion); it does
not provide a generic problem→script lookup that would justify a "Use
Scripts, Not Grep" table entry, and it does not generate a fixed canonical
artifact path.

**Next Step**: Run `task-complete` to mark the task done and commit.
