# Protocol: TASK-PROC-006-11 wire-monitors-into-task-complete

## 2026-05-28T00:00:00Z
**Agent**: Main session (claude-route → task-resolve)
**Agent ID**: 13c9f023-be5c-4488-8550-0463a2000ad4
**Action**: Implemented IMPL-F — wired run_monitors.py into task-complete skill

**Outcome**: PASS

Deliverables completed:
1. `.claude/skills/task-complete/SKILL.md` — added step 6 "Run Monitor Sweep" after the commit step (step 5). Invokes `python3 scripts/optimize/run_monitors.py`. Skips when `SKIP_QUALITY_GATES=1` (proxy for back-out / force paths). Captures exit code; logs and continues on failure (best-effort).
2. `scripts/tests/test_task_complete_wiring.py` — 4 smoke tests verifying:
   - SKILL.md references run_monitors.py
   - Monitor wiring is post-commit
   - SKIP_QUALITY_GATES=1 skip condition is documented
   - Failure-continuation is documented
3. All 4 ACs checked off in goal.md.

Python gates:
- G1 lint: PASS
- G2 type: PASS  
- G3 tests: Pre-existing failure in test_orchestrate.py (CLAUDE_SESSION_ID env var set in automated session); 0 new failures from this change; new 4 tests all PASS
- G4 no-handrolled: PASS
- G5 print discipline: PASS

**Next Step**: doc-update-guidelines → task-complete
