# Plan: Wire run_monitors.py into task-complete (IMPL-F)

## Approach: Inline

## Deliverables

1. Edit `.claude/skills/task-complete/SKILL.md` to add step 6 (monitor sweep)
   - After step 5 (commit), append invocation of `python3 scripts/optimize/run_monitors.py`
   - Best-effort: capture exit code + stderr, log, continue — never abort task-complete
   - Skip when `SKIP_QUALITY_GATES=1` is set (proxy for dry-run / force / back-out paths)
   - Document the skip decision inline at the wiring point

## Skip Condition Decision

`SKIP_QUALITY_GATES=1` covers the "back-out / WIP escalation" path (set by
pending_feedback escalation and explicit user bypass). Monitors on back-out
paths would feed events from incomplete tasks — so skipping here is correct.
The `--force` concept from goal.md maps to this same env-var gate in the
current skill design.

## Phases

1. Edit SKILL.md → verify step added, skip condition documented
2. claude-log → persist session
3. doc-update-guidelines → check for guideline updates
4. task-complete → close out
