2. Simulation-based fast evaluation

Replay of real logged tasks: Some thoughts to that. I think we should use Git to create branches that point to such test data sets. That way we can always revert to that commit and run another test round if we want. But it's important that there is a mechanism that cleans those branches up as well. And a good naming so that we always know which branches are which and what can be deleted and what has to stay and so on. For the synthetic tests we could use the same mechanism and also create branches that point to testing data sets. What I don't like is creating synthetic data from nothing. I think we should always wait for real data. So we need first a real data set to run the tests. And from those real data we can derive synthesized new synthetic test data.
But I think that's not necessarily part of the Or maybe we should already do that in the requirements. I think the workflow improvement automation requirement is getting so large that we have to make an epic out of it. And we can create feature requirements for the specific details. 
Let's do that and create new evaluation exploration tasks For each feature. 

Or the overall epic level I think we now have enough and reach the point where we can close that.

So your goal now is to elevate this feature to an epic and restructure it. But don't do it in inline. Instead use agents to do it. Cause all tasks that currently exist must also be distributed. Oh no, thinking about it we can just keep them on epic level. But the tasks that are pending maybe they should be distributed to the correct features. And of course we still have to update those tasks. But as I said, I think there are some details still that need to be fletched out. So I think I want to defer that and instead as I said create for each feature a new exploration task. That has to go to do what we never did here, basically. To uh fill in the gaps and iterate a bit more before we then modify the implementation task. So everything can be implemented. Those exploration tasks must of course reference all the iteration loop documents we created so far in this exploration task we are currently working on and also reference old information from old explorations. So that it has the whole picture.

Please don't just do the restructuring, create a plan first and ask me for approval. Think about a good structure and justify your decisions.


But I can also answer your three questions:
For the weekly trigger mechanism. I follow your recommendation. Let's not over engineer it.

Yes, once a scenario is created. It's put on the denial list. So that it cannot be edited. But of course there needs to be a way to add more scenarios and deprecate and or delete old ones. Yeah, we have to come up with a mechanism that cannot be tricked easily.

Simulation cost is a very tricky topic. Um It really depends on the skill we are testing. I also wonder how we want to deal with iterative skills that rely on user feedback. I guess we just do one iteration, right?But for example the script scale consumes a lot of tokens because it generates multiple HTML files.I think we need an additional budget for simulation.And this budget should be dynamic. Should be based on the skills that are tested.But we have to try it out first. So I think for the first iteration for the first time a skill is simulated, we don't set a budget and measure it.So in the future we can we have a baseline and know how much one iteration costs. So we can say okay we just allow three iterations and start with that and then if we notice That it has too much or not enough, we can adapt it.