simulation like skill-creator does it. see: TASK-PROC-055-06
that could make the measurement of improvements easier. we probably can't use the skill 1:1...

"Account-local (round-4 §1.4): session files are per-account" => no, we're using ccs. all sessions are stored in a shared folder.

the 4 MB are not a hard cut. the session/task that exceeds it can complete, but if size > 4MB no new task must be started.

5.1 Weekly self-run cadence: how do we trigger it weekly? do we need to allow a date in the tasks awaits field that is respected by next-task.py? (note: every week means every week work on the project is done, weeks can be skipped if the project is not touched)

i love your ideation results!

Contract tuning constant: yes

B4 — make developer-review capacity an explicit budget? => no, not now. maybe in the future.

good round. we need another one!