---
task_id: TASK-PROC-069-03
date: 2026-06-05
phase: execution
skills_used:
  - task-start
  - claude-route
  - task-resolve
  - claude-modify-skill
  - claude-write-script
  - claude-log
  - task-complete
  - claude-commit
---

# Protocol — Shrink claude-route + Migrate Callers

## AC-06 Audit Finding (orchestrate.py)

**Confirmed correct — no logic change needed.**

- `find_resumable_in_progress_task` (L1597–1611): Searches for `in_progress` tasks with
  `session_id`. task-start now writes both (P3). Any task started via task-start has
  `in_progress` + `session_id` set before delegation. The resume path will correctly find
  these sessions. Logic is sound.
- Fresh-session path (L1610): launches "Do next task" → task-start. So all automated
  execution paths go through task-start. AC-06 satisfied.
- L2121 comment: "bypassed claude-route" → "bypassed task-start" (comment-only update;
  task-start now owns the session_id write that the resume-UUID depends on).

## Migration Tracker

| Unit | File | Change | Agent | Status |
|------|------|--------|-------|--------|
| U1 | `claude-route/SKILL.md` | Remove Mode A steps 1-2b, Mode B, Mode C; shrink to pure router | Agent A | pending |
| U2 | `claude-route/contract.yaml` | Narrow required-input to validated in_progress path | Agent A (via claude-modify-skill step 4b) | pending |
| U3 | `goal_metadata.yaml` L9,45,74 | L9 add task-start to Consumed by; L45 add task-start→; L74 "Set by task-start" | Inline | pending |
| U4 | `pending_question.yaml` L32 | "Written by claude-route" → "task-start" | Inline | pending |
| U5 | `terminate_session.sh` L5 | "bypass claude-route" → "bypass task-start" | Inline | pending |
| U6 | `orchestrate.py` L2121 | Comment: "bypassed claude-route" → "bypassed task-start" | Agent B (claude-write-script) | pending |
| U7 | `create_optimize_cycle_task.py` L14,138 | "via claude-route" → "via task-start → claude-route" | Agent B (claude-write-script) | pending |
| U8 | `test_next_tasks.py` L6 | "used by claude-route" → "used by task-start (via claude-route)" | Agent B (claude-write-script) | pending |
| U9 | `task-derive-from-requ/SKILL.md` | Minor: "via claude-route detection" update | Agent C | pending |
| U10 | `task-complete/SKILL.md` | Minor: skills_used guidance add task-start | Agent C | pending |
| U11 | `verify-quality/SKILL.md` | "not routed through claude-route" → "task-start" | Agent C | pending |
| U12 | `task-create-code/SKILL.md` | Factory position chain: add task-start→ | Agent C | pending |

## Agents

- **Agent A** (background): claude-modify-skill on claude-route — structural shrink + contract
- **[Review point]**: User reviews Agent A diff (claude-route SKILL.md + contract.yaml)
- **Agent B** (background, after A or parallel): 3 Python script comment edits via claude-write-script
- **Agent C** (background, after A — shares INDEX.md/factory_flows.md): 4 minor skill updates via claude-modify-skill

## Verification (after all agents)

- `grep -rn "claude-route" .claude/ scripts/ CLAUDE.md` — only internal-router refs remain
- Spot-check code-simple, code-complex, code-test for AC-05 defense-in-depth pre-checks
