---
skills_used:
  - claude-route
  - task-resolve
  - claude-write-script
  - doc-update-guidelines
  - task-complete
  - claude-commit
---

# Protocol — TASK-PROC-044-15: Reconcile schemas with corpus

## Decisions

### DEFECT-1: `type` enum in goal_metadata.yaml
Legacy values found in corpus: `analyze`(12), `review`(3), `define`(2), `explore+impl`(1).
**Decision: treat as legacy corpus entries — NOT added to enum.**
The existing schema description already states: "(define/review/analyze were aliases for explore;
folded in — use explore instead)." Confirmed and extended: `explore+impl` is also a legacy value
that should be split into separate tasks. The repaired real-corpus smoke test (DEFECT-4) will
guard against new occurrences.

### DEFECT-2: `status: completed` in requirements_frontmatter.yaml
Found in 1 corpus file. The lifecycle policy (`requirements_and_tasks/requirements.md`) does NOT
include `completed` as a valid requirement status — only tasks have `completed`. Adding to schema
as a legacy/irregular value with a comment.

### DEFECT-2: `status: draft` in requirements_frontmatter.yaml
Found in 1 corpus file. The lifecycle policy DOES include `draft` as a valid initial state.
Adding to schema enum.

### DEFECT-3: `target_package` pattern
Removing the bogus `PKG-x.y.z-name` pattern entirely. Real values are free-form package names
from RELEASE_BACKLOG.md (e.g. "Transfer Data Model", "claude-optimize"). Aligned with
`requirements_frontmatter.yaml` which already documents this correctly.

## Execution log

- [x] Phase 1: DEFECT-1 + DEFECT-3 → goal_metadata.yaml (added 39 optional fields, removed bogus PKG- pattern)
- [x] Phase 2: DEFECT-2 → requirements_frontmatter.yaml (added 18 fields, fixed status/effort enums)
- [x] Phase 3: DEFECT-4 → claude-write-script for test repair (replaced single brittle path with full corpus iteration)
- [x] Phase 4: Verified — goal.md 123→26 (all documented legacy), requirements.md 155→0, G1-G5 all pass
