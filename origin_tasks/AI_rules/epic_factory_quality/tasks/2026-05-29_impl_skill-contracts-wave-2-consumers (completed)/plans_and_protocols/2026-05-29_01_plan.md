# Wave 2 — Plan (TASK-PROC-044-04)

**Approach:** inline. Cross-contract named-producer references (`source: skill:<name>`)
must stay mutually consistent for `check_skill_contracts.py` to pass; that consistency is
cheapest to guarantee in a single coherent context. All patterns already gathered from
Wave 1 deliverables + exploration synthesis (D-1 rubric, D-2 contract shape, D-5 wave order).

## Reference patterns (loaded)
- Contract shape: `.claude/skills/task-create/contract.yaml` (canonical full field set).
- Schema dialect: `.claude/schemas/goal_metadata.yaml` (flat YAML `required:`/`optional:`,
  per-key `type`/`enum`/`pattern`). NOT JSON Schema.
- Lint: `scripts/quality/check_skill_contracts.py` — rules: derived_from↔produces xref,
  named-producer (basename match), may_invoke folder exists, schema: file exists.
- Pre-check pattern: `ui-verify-flutter` Wave-1 fixture (5-line bash, exit 2 on missing input).
- Rubric (Round 1 §3.1): S1 standalone-invocable, S2 coordinates ≥2 agents, S3 natural
  human review/decision boundary, S4 file artifact crosses boundary. ≥2 YES → sub-skill.

## Phases
1. **Validator** — `scripts/quality/validate_against_schema.py` via `claude-write-script`
   (tier B). Reads artifact YAML + schema YAML; checks required keys present, unknown keys
   flagged, enum membership, basic type. Exit 0 pass / 1 with specific message. + pytest.
   Success: Python gates G1–G3 PASS; validator runs green against a real goal.md.
2. **Contracts** — author `contract.yaml` for the 9 consumers mirroring task-create style.
   Named-producer refs: goal.md → skill:task-create / task-create-code / task-derive-from-requ;
   requirements.md → skill:requ-explore; scribbles → skill:ui-create-scribble.
   Success: `check_skill_contracts.py` → PASS, 0 violations.
3. **Pre-checks** — add 5-line bash pre-check at top of each of the 9 skill bodies
   (claude-modify-skill steps; minor-logic change, no INDEX/factory_flows sync). ui-verify
   already has the fixture — upgrade it to call the validator.
4. **Rubric re-run** — one entry per skill (S1–S4 + score + verdict) in
   `plans_and_protocols/2026-05-29_03_rubric_rerun.md`. If any borderline/surprise →
   revision_target.yaml into FU-6 (TASK-PROC-044-08) workspace.
5. **Verify** — lint + validator + python gates + verify-quality (no lib/test touched, but
   run for discipline). Then claude-log, doc-update-guidelines, task-complete.

## Notes
- No lib/test/integration_test files touched → Dart gates N/A; Python gates apply to validator.
- contract.yaml are new sidecars (as Wave 1 producers were); pre-check edits go via
  claude-modify-skill procedure.
