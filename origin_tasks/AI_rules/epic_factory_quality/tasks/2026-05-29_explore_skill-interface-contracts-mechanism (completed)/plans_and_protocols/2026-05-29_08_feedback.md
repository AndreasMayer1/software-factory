I want to question the naming of the agent and skills. I like that we have this UI prefix in front of the scribble skills. I want to have the same for the ancients to make clear that they belong together. Then we have the Persona Embodiment Reviewer. I see one potential problem and that is that the name suggests that it's not only relevant for the scribbles, but that it is a general reviewer that reviews something, maybe anything, from the perspective of a persona. I mean we also have a app provider persona. Um that would mean that uh this agent could potentially review also the software factory because the app provider is interested in the workings of the software factory. So the name does not make sense. It's too generic. We have to think about what to what it belongs. It should start with the domain or the scope in which it operates. Like we have for the UI scribble. Here the domain is the user interface. And then with the scribble we say okay, we are scribbling something for the user interface. So I think for the reviewer we should also start with the user interface. And now the question is does it only review scribbles or does it also review flutter implementation? Think about it. 
Then we have the Scribble UX Protocol Reviewer. What is the UX protocol? I don't like the term, it's also too uh broad. Also the Scribble Auto Reviewer. Autor review. It's automatic, I guess. But I mean every agent is automatic. Right? But of course it's just the naming. Changing the names later is an easy task.

I approve everything if not stated otherwise.

Before you implent anything: I have a question: revision_target.yaml for skill -> skill communication. Explain how that works in regards of the sessions. Session A runs skill X. it surfaces a request for skill Y. it writes revision_target.yaml. and now? does session A call skill Y directly in the same session to continue? does it start an agent? Does it terminate the session? but whn it terminates, who is going to start a new session to execute skill Y? remember: new sessions are only started if there is a pending task to start. (or the user starts a session manually)

The 60-day sunset is a target, not a guarantee: we can "enforce" it by adding the tasks to flutter_app/.claude/task_ordering_priority_override.txt no problem. we have to do it now anyways, lets do the whole migration before moving on with the scribble skill (which again block the 0.0.1 release work).

The factory-map interpretation of AC-06: i like it! it's basically what we tried with flutter_app/.claude/factory_flows.md before but automatic. it should be much better in displaying all artifacts and the skills as edges. maybe there is a nice graph js framework we could use that also allows filters to be applied on the graph (with all artifacts and skills it will be very overwhelming visually). of course such a js tool would need the graph in a special format, we'd have to align...
do you think we should also include the script calls? little hidden question here thar opens another large box... 


Token-cost of contract.yaml + schemas => how would you measure that? hooks that count file reads by the llm? would be interesting for other files as well. is the factory / skill optimize mechanism interested in such a metric?

" Re-run the rubric per-family during Wave 2/3 migration; expect refinements." => add it to the task

before you create the task: read flutter_app/.claude/skills/task-derive-from-requ/SKILL.md