# Session Kickoff — TASK-PROC-044-02

**Date:** 2026-05-29 · **Model:** Opus 4.7 · **Mode:** manual interactive

> Captures what was loaded into context before Round 1 begins. Future agents reading this can skip the full re-read of the 5 iteration documents and pick up at §"What is settled" / §"What is open".

---

## 1. Reading completed (Phase 1)

### From TASK-PROC-032-10's `plans_and_protocols/`

1. **File 08** — `2026-05-28_08_skill_interface_exploration.md` (agent-produced inventory + initial mechanism proposals; explicitly INCOMPLETE per developer)
2. **File 09** — `2026-05-29_09_design_thinking_iteration_4.md` (integration; bundles; dependency graph)
3. **File 10** — `2026-05-29_10_redundancy_check.md` (R3 collapse, R9 gap-fill `claude-modify-agent`, sharp boundaries)
4. **File 12** — `2026-05-29_12_design_thinking_iteration_5.md` (REQ-PROC-044 alignment, D41/D42 dropped, PRINCE2 evaluation, total-cost framing)
5. **File 13** — `2026-05-29_13_session_token_efficiency_analysis.md` (session-vs-agent decision tree, recommended cuts)
6. **File 14** — `2026-05-29_14_feedback.md` (the feedback that triggered spinning out this task)

### Other context

- `REQ-PROC-044/requirements.md` — 6 ACs the mechanism must operationalize
- `.claude/factory_flows.md` — information-flow diagram
- `.claude/skills/INDEX.md` — current skill registry (60 skills + INDEX.md)
- `TASK-PROC-057-01` goal.md — apex factory-purpose exploration, `status: pending` (NOT in_progress); awareness only
- `requirements_tasks/STATUS.md` — open task inventory; TASK-PROC-032-10 is `in_progress` and waiting on us
- `automation/` directory — confirmed channels: `pending_feedback/`, `dependency_reviews/`, `answered_feedback/`, `inbox.md`, `REFACTOR_ISSUES.md`, `MONITORING_CRITERIA.md`, `session_outputs/`, `reports/`, `orchestrate.log`, `state.json`, `scheduled_launch.sh`
- `.factory/optimize/events/` — improvement-event channel consumed by `claude-optimize`

### Existing review/feedback channels found (corrects file 08's count)

| Channel | Location | Producer | Consumer |
|---|---|---|---|
| Developer questions | `automation/pending_feedback/<task-id>/question.md` | Skills via 5-cycle back-pressure (CLAUDE.md §7) | Developer answers; orchestrator resumes |
| Answered feedback (archive) | `automation/answered_feedback/` | Developer | Audit trail |
| Dependency reviews | `automation/dependency_reviews/` | Skills via REQ-PROC-060 admission gate | Developer |
| Optimize events | `.factory/optimize/events/*.json` | Skills emit on file change events | `claude-optimize` skill |
| Refactor issues | `automation/REFACTOR_ISSUES.md` | Free-form developer notes | — |
| Inbox | `automation/inbox.md` | Free-form developer notes | — |

This is the *actual* current channel landscape — file 08's "three channels" framing was incomplete.

---

## 2. Factory size (corrects file 08's "~70 skills")

```
.claude/skills/  → 60 skills total (excluding INDEX.md)
```

Families:
- `code-*` (6): bugfix, complex, run-integration, simple, test
- `requ-*` (5): apply-market, assign-packages, derive-from-flow, explore, merge, verify-flow-coverage
- `ui-*` (4): create-scribble, create-scribble-improve, improve-flutter, verify-flutter
- `ux-*` (8): create-flow (+ 3 sub-skills flow-draft/complete/approve), validate-rule, write-canon-concept, write-persona, write-scenario, vcd-log-tradeoff
- `task-*` (8): complete, complete-bugfix, create, create-code, derive-from-requ, repair-meta, resolve, unblock-check
- `doc-*` (4): lookup-dependencies, split, update-guidelines, update-tokens
- `claude-*` (15): ask, automated-mode, autorun, commit, create-skill, install-os-tool, log, modify-ordering-rules, modify-skill, optimize, optimize-audit, resume-agent, route, save-checkpoint, write-script
- `release-*` (5): begin-impl, begin-impl-finalize, plan, release-status, release
- Misc (5): brb, codegraph, product-intake, verify-quality

---

## 3. What is SETTLED coming into Round 1 (do not re-litigate)

These were resolved by file 10 (redundancy check) and file 12 (round-5 strategic narrowing). Round 1 starts from here.

| # | Resolved | Source |
|---|---|---|
| S-1 | **Parent requirement**: REQ-PROC-044 (not a new requirement) | file 12 §2.1; goal.md frontmatter |
| S-2 | **No new frontmatter fields**: `presentation_layer:` (D41) and `serves_requirements:` (D42) DROPPED — auto-discovery script uses heuristic + reads `requirements_matrix.md` | file 12 §3 |
| S-3 | **Tasks are the only integration mechanism** (file 12 S2) — handoff files are dead ends; every bundle must end with task creation. "Defer to backlog" = create a task. | file 14 |
| S-4 | **Infrastructure-first total-cost decision**: this task is added to `task_ordering_priority_override.txt` ahead of 0.0.1 implementation work | file 12 §7; already done |
| S-5 | **Bidirectional feedback = one pattern, three triggers** (coder→scribble, reviewer→flow, validator→scribble); file-based channel; same 5-cycle escalation as `verify-quality` | file 08 §5, file 09 §4 |
| S-6 | **Persona-embodiment**: read 1 scenario per affected persona via flow's `serves_scenarios` mapping (no name-matching) | file 09 §5.1, file 12 §5.2 |
| S-7 | **Hybrid model is the position**: artifacts are first-class (schemas), skills retain workflow ownership (contracts reference schemas), PRINCE2-influenced | file 12 §4 |
| S-8 | **Session-split rules** (file 13 §4): SPLIT at user-interaction boundaries (Phase 4 of scribble); AGENT-INVOKE at LLM-activation boundaries (specialized vocabulary); DON'T SPLIT iteration cycles or read-overlapping phases |

---

## 4. What is OPEN — the 6 decisions this task must resolve

Reproduced from goal.md §"Decisions to Resolve" for working reference:

| # | Decision | Type |
|---|---|---|
| O-1 | Sub-skill split for the scribble pipeline — confirm or revise file 09 §7.1's 4-sub-skill cut, applying file 14's "sub-skill vs agent" rubric (seed 5) | DESIGN |
| O-2 | Contract mechanism — sidecar `contract.yaml` / inline frontmatter / registry / hybrid | DESIGN |
| O-3 | Bidirectional feedback channel structure — unified (revision_requests nested under pending_feedback) OR separate channels with orchestrator update | DESIGN |
| O-4 | `revision_request` vs creating a task taxonomy — final rule for each | DESIGN |
| O-5 | Migration sequence — first family, opt-out `contract_version: 0`, duplicate cleanup | OPERATIONAL |
| O-6 | Does REQ-PROC-044 need new ACs? — propose via separate follow-up `requ-explore` if yes | META |

---

## 5. What needs validation in this task

### From file 14 (the round-6 developer feedback that spun this off):

- **§4.4 audit obligation**: most artifacts ALREADY have definitions (README files at their folder roots describing the shape). Migration must remove stale duplicates — the new mechanism must NOT coexist with the old prose specs. Round 2 prototype phase must demonstrate this cleanup on at least one artifact.
- **revision_request more general?** file 14: skills today use `pending_feedback/` for iterative-review proposals (asking "is this good enough?"). A new `revision_requests/` folder risks orchestrator interference (today the orchestrator scans only pending_feedback/). **Round 3 must propose either (a) nested under pending_feedback/ as a subfolder OR (b) separate channel with explicit orchestrator scan update.**
- **factory-folder improvement suggestions** also exist (`.factory/optimize/events/` confirmed). Does that channel merge too, or stay separate? Round 3 answer.
- **"defer to backlog"** — backlog lives in the task list; not a separate file. file 14 line 8.
- **requirements_matrix.md may not exist** — see `requ-derive-from-flow` skill for the existing handling.
- **TASK-PROC-057-01 (apex) awareness** — currently `pending`, not running. Low-risk to ignore for now; integrate constraints if it lands during execution.
- **Sub-skill vs agent rubric** (file 14 last paragraph): if a sub-skill only spawns an agent, replace the sub-skill with the agent. Apply concretely to SCRIBBLE-SPLIT's 4-cut.

### From REQ-PROC-044 directly:

The mechanism we recommend must operationalize ACs 01–06. Round 3 must include a per-AC mapping showing how the recommended mechanism satisfies each AC.

---

## 6. Strategic plan for the exploration

### Phase split (this session)

| Round | Mode | Deliverable | Notes |
|---|---|---|---|
| **Round 1** | Main session synthesis | Problem-space framing: 3 concrete real-world failure scenarios; sub-skill-vs-agent rubric applied to SCRIBBLE-SPLIT | Builds on file 08 §1 inventory (already in context) |
| **Round 2** | Delegated agents (web research + prototype) | (a) Deeper web research on multi-agent contracts (acknowledged incomplete); (b) concrete YAML on ≥2 skills + ≤80-line lint script catching ≥1 real violation | Parallel background agents; main session keeps cache warm with `/loop` heartbeat |
| **Round 3** | Main session synthesis | Resolve the 6 OPEN decisions; per-AC mapping; migration sequence; explicit honest-uncertainty list | All decisions framed clearly enough for the user to confirm/revise |
| **Phase 3** | Interactive | Present synthesis to the user; iterate per feedback | Per goal.md §"Acceptance Criteria": "Decisions requiring user input are identified and framed clearly enough for the user to decide" |
| **Phase 4** | Main session | Create follow-up tasks via `task-create` (per S-3); tick ACs; `task-complete` | The rollout work the recommendation implies |

### Agent delegation plan (file 13 §6 applied)

- **Web research** — delegate to `general-purpose` agent (foreground; output is small summary; ~10 web fetches estimated <5 min)
- **Cross-skill inventory deepening** — main session covers the families already inventoried in file 08; only delegate if I find gaps (defer decision to end of Round 1)
- **Prototype YAML + lint script** — delegate to `implementation-engineer` agent (background if >5 min; mandatory `/loop` heartbeat per CLAUDE.md cache rule)
- **Synthesis rounds** — STAY in main session (context already loaded from file 08-14)

---

## 7. What this task is NOT

To avoid scope drift:

- **Not** writing or amending REQ-PROC-044 itself (`writes_requirements: false`)
- **Not** executing any of the 10 deferred bundles from TASK-PROC-032-10
- **Not** mandating any specific Flutter feature policy (file 12 §5.3 — deferred until parity lint produces evidence)
- **Not** producing a final/locked migration sequence — Round 3's migration recommendation can be revised at executor-level
- **Not** auto-launching the SCRIBBLE-SPLIT work — that's a follow-up task per S-3

---

## 8. Honesty/uncertainty tracker (carried throughout)

- Token estimates in file 13 are uninstrumented; treat as relative magnitudes
- The 6 decisions are framed but not pre-decided; the user may reverse any
- The migration sequence will need real-world adjustment as the first 2–3 skills migrate
- The "sub-skill vs agent" rubric needs prototype validation before being baked into `claude-create-skill`
- File 08's external-pattern survey was acknowledged incomplete; Round 2 deepens it
