---
skills_used:
  - requ-explore
  - task-complete
  - claude-commit
---

# Protocol — Phase 1 (requirement authoring) of the TASK-PROC-032-16 remediation

**Date:** 2026-05-31 · **Model:** Opus 4.8 · **Branch:** develop
**Scope:** Two sequential requirement-authoring passes from remediation plan §A1, §A2, §B (N0), §C1b, §F. No task-complete run (orchestrator verifies/completes).

---

## PASS 1 — N0 (TASK-PROC-044-16): epic promotion + new feature requirement

### Blast-radius assessment (epic rename guardrail)
Before renaming, grepped the repo for `factory_quality`:
- **No references** in CLAUDE.md, `.claude/` skills, `scripts/`, or `.claude/task_ordering_priority_override.txt`.
- **3 live requirement cross-ref links** (relative paths to `factory_quality/requirements.md`): `scripts_organization`, `requirements_structure_quality`, `ui_sketch_iteration_workflow`.
- **Generated files** (`requirements.md` merged, `_meta/id_registry.md`): regenerated by scripts after the move — not hand-edited at source-of-truth level.
- **Historical task protocols / completed-task goals** and `automation/reports/`: immutable history — NOT edited.
- **`.factory/optimize/events/*.json`**: generated instrumentation, pruned on schedule — not authoritative.
- **`git show <commit>:...factory_quality/...` lines** in two goal.md files: git-revision pins where the path WAS `factory_quality` at that commit — correctly left unchanged.

Conclusion: blast radius **safe and confined**. Proceeded with E1 (epic prefix-rename). Escape hatch NOT used.

### Actions
1. `git mv requirements_tasks/process/AI_rules/factory_quality → .../epic_factory_quality` — history preserved; 16 task folders moved with the parent (63 tracked files now under new path, 0 under old). Task IDs unchanged (e.g. TASK-PROC-044-16 stays 044-16). REQ id stays REQ-PROC-044.
2. Allocated feature REQ-ID via `allocate_req_id.py --parent-id REQ-PROC-044 --parent-path .../epic_factory_quality` → **REQ-PROC-044-01**; reserve marker deleted after writing.
3. Rewrote the epic body (`epic_factory_quality/requirements.md`) to a clean epic shape: Overview / Purpose / Scope / Features (index) / Cross-Feature Invariants (the 8 ACs as epic-level invariants) / User Needs / Dependencies / References. **Body = 46 lines** (≤90 gate). The 8 ACs (AC-01..AC-08) stay in frontmatter as cross-cutting invariants. Title changed to "Epic: Software Factory Quality".
4. Authored the new feature requirement at `epic_factory_quality/feat_capability_authoring_skills/requirements.md`, `id: REQ-PROC-044-01`, `status: active` (living document). ACs:
   - AC-01 — agent authoring governed by `claude-create-agent`/`claude-modify-agent` (collision-checked naming vs built-ins/Han imports/existing agents; `allowed_tools` by intent class, no bare `*` without justification; when-to-create gate; required sections; agent-vs-session suitability check).
   - AC-02 — ≤50-token role identity + `## Domain Vocabulary`, `## Anti-Patterns`, `## Protocols`, `## Output`, `## Rules`.
   - AC-03 — Domain-Vocabulary authoring aid: 10–25 expert-tier terms passing the "15-year practitioner test"; shallow vocabulary rejected (capability D9).
   - AC-04 — contract-mechanism integration (emit `contract.yaml`, follow split rubric REQ-PROC-044 AC-03 by reference).
   - AC-05 — single ownership of the 6 meta-skills (`claude-create-skill`, `-modify-skill`, `-create-agent`, `-modify-agent`, `-write-script`, `-modify-ordering-rules`) by cross-link, not restatement.
5. Updated the 3 live cross-ref links to point at `epic_factory_quality/requirements.md`.
6. Regenerated `_meta/id_registry.md` (now lists `process/AI_rules/epic_factory_quality`) and the merged `requirements.md` (auto-committed by the merge script as `f3c15ed8`, which also folded in the git mv rename + epic/feature edits made up to that point).

### Notes / decisions
- The ID registry lists only top-level requirement IDs; **no** feature/derived `-NN` IDs appear for ANY feature (verified: `REQ-FUNC-007-01` etc. also absent). REQ-PROC-044-01 not appearing in the registry is expected, not a defect.
- `requirements_version.file` paths in moved task goals are relative (`../requirements.md` / `../../requirements.md`) and resolve correctly because task folders moved with the parent.
- TASK-PROC-044-16 left `status: in_progress` (not completed). It is ready: its AC-05 ("REQ-PROC-044 promoted to epic and feat_capability_authoring_skills/requirements.md exists with the three AC groups") is satisfied by this work.

### Final paths (Pass 1)
- Epic (renamed): `requirements_tasks/process/AI_rules/epic_factory_quality/requirements.md` (REQ-PROC-044).
- New feature: `requirements_tasks/process/AI_rules/epic_factory_quality/feat_capability_authoring_skills/requirements.md` (**REQ-PROC-044-01**, status active).

---

## PASS 2 — REQ-PROC-032: AC-37..AC-41 + amend AC-23

### Task created
`task-create` (explore, REQ-PROC-032, writes_requirements: true, opus_recommended: true):
- **TASK-PROC-032-21** at `.../ui_sketch_iteration_workflow/tasks/2026-05-31_explore_extend-scribble-contract-recovered-acs/`, `status: in_progress` (not completed).
- Added to `.claude/task_ordering_priority_override.txt` under the existing "TASK-PROC-032-16 review remediation" section, with its own comment line above the ID (column-0 comment, no inline comment), after `TASK-PROC-044-16`.

### Requirement edits (requ-explore discipline; process requirement, NO target_package)
Added to `trackable_items.acceptance_criteria` (next free id was AC-37) + matching body prose:
- **AC-37** Scribble storage mirrors lib/features/ — body in `## Storage and Organization` (new "Storage layout mirrors lib/features/" block + supersede note for the legacy co-located path). (D33–36)
- **AC-38** Per-flow navigation captured (`flow_navigation.yaml`) — body in `## Scribble–Coder Contract`. (D20)
- **AC-39** Per-flow walk validation before approval — body in `## Scribble Review Doctrine`; header range updated to `[AC-28..AC-31, AC-39]`. (D39)
- **AC-40** Approval trail (`APPROVAL_TRAIL.md`) — body in `## Scribble–Coder Contract`. (D43)
- **AC-41** Contributing-requirements / participating-flows discovery (no new frontmatter; fields already in `.claude/schemas/scribble_metadata.yaml`) — body bullet in `## Scribble Content Extensions`; header range updated to `[AC-32..AC-36, AC-41]`. (D29/D30/D40)
- **AC-23 amended** (frontmatter description + body line under `## Scribble–Coder Contract`): `flutter_handoff.yaml` also carries a `design_decisions:` block propagating scribble metadata `design_decisions`, validated by `.claude/schemas/flutter_handoff.yaml`. (D8)

No new heading was introduced, so no new `sections` (SEC) entries were needed. AC-21 ("What a Scribble Commits To") remains the single normative contract source.

### Validation
- All three requirement YAMLs parse (PyYAML). REQ-PROC-032 now has 41 ACs (AC-37..AC-41 present, last).
- Transition-language scan of new AC descriptions: clean (no replace/migrate/convert/refactor as a step). End-state phrasing used throughout.
- 5 body AC tags ([AC-37]..[AC-41]) present.
- Regenerated `_meta/id_registry.md`.

---

## Blockers / things for the orchestrator
- **None blocking.** Nothing was committed for Pass 2 or for the post-merge Pass-1 edits — orchestrator owns `verify` + commit per instructions.
- The merge script already committed `f3c15ed8` (merged requirements.md + the git mv rename). Remaining uncommitted: priority override, id_registry, epic requirements.md, the 3 cross-ref edits, ui_sketch_iteration_workflow/requirements.md, the new feature folder, the two new task folders. Stage and commit these together (separate `git add` then commit, per CLAUDE.md). Re-running `merge_requirements.py` after the REQ-PROC-032 edits would refresh the merged file (it currently predates the AC-37..41 additions).
- TASK-PROC-044-16 and TASK-PROC-032-21 both remain `in_progress` by design.
- AC-37 depends on a `lib/features/` structure policy that is itself underspecified (remediation plan P0). The AC is phrased so the parity check does not over-commit to a not-yet-existing policy; P0 is a separate downstream task, not part of Phase 1.
