# Kickoff — TASK-PROC-044-10 External-Interface Contracts (FU-8)

**Date:** 2026-05-30 · **Model:** Opus 4.8 · **Mode:** automated
**Session:** f2fdd72b-f3fd-4220-aac5-f2fba94a1066 (gmail)

## What this task is

Pure exploration (`writes_requirements: false`). The internal skill-interface-contract
mechanism (REQ-PROC-044, rolled out by FU-1..FU-7) covers skill→skill edges *inside* the
factory. This task extends the same contract format to the factory's *external* boundary
(E1–E9 inventoried by the parent exploration in `07_external_interfaces.md`) and decides
where external-interface declarations should live.

## Inputs read

- `07_external_interfaces.md` (parent TASK-PROC-044-02) — E1–E9 inventory + compatibility verdict (format extends cleanly, zero rework risk).
- `10_task_creation_plan.md` — FU-1..FU-8 plan; FU-8 = this task.
- Internal contract format: `.claude/skills/*/contract.yaml` (13 exist), schema dialect `.claude/schemas/*.yaml`, lint `scripts/quality/check_skill_contracts.py`.
- REQ-PROC-044 requirement (AC-01..AC-06, status active, living document).

## Inventory verification (goal step 2) — channels beyond E1–E9

Walked the factory tree (`grep` over `.claude/skills/` for git-remote, signing, ctx7, WebFetch, install channels):

- **E4 refinement** — web research has two distinct modalities: the `ctx7` CLI (structured docs via `command_output`, used by `doc-lookup-dependencies`/`find-docs`) and raw `WebFetch`/`WebSearch`/`curl` (`url_response`). Both are E4 but need different postcondition checks.
- **E10 (new)** — Git-remote interaction (push to `origin/develop`, tag push, `gh` ops). Direction: factory → remote VCS. **Today developer-operated** — no skill issues `git push` automatically (commits land on local `develop`; release tagging/push is manual). Documented; validator deferred (YAGNI — no automated channel yet).
- **E11 (new)** — Windows-host build/test bridge. Direction: factory → developer-on-Windows-host → factory (NTFS mirror). The automated `win-command-bridge` was **deleted by TASK-PROC-054-02**; Windows ops are now manually developer-initiated. An external boundary that exists but has no automated channel — documented, no contract authored.
- E8 (runtime telemetry) confirmed hypothetical / out of scope (REQ-PROC-052 forbids telemetry SDKs today).

## Deliverables plan

1. **Validator scripts (8, tier-B, ≤30 lines)** in `scripts/factory/external_state/` — implement the postcondition vocabulary. Created via `claude-write-script`; must pass Python gates.
2. **Vocabulary doc** `scripts/factory/external_state/README.md` — ≥5 entries, rationale + validator mapping per entry.
3. **≥3 concrete contract declarations** (dev-input E1, web E4, OS E5) — in this task's plans_and_protocols/ (placement is rollout work, a follow-up).
4. **Additive-field analysis** — `input_modality:` for the internal format (covers product-intake conversation input + command_output/url_response).
5. **REQ-home recommendation** — fold under REQ-PROC-044 vs new sibling.
6. **Follow-up tasks** via task-create for the rollout.
7. **Synthesis** doc — including what could NOT be validated.

## REQ-home leaning (to be argued in synthesis)

Fold under REQ-PROC-044 (one mechanism = one requirement; AC-02 end-to-end traceability
*requires* external edges; same single-developer stakeholder). Concrete next step is a
`requ-explore` follow-up to add an external-boundary AC — **not** `product-intake`, because
REQ-PROC-044 is a process/factory requirement (stakeholder app_provider), not a user-facing
app change flowing through the persona chain. (The goal text suggested product-intake; the
exploration's finding refines this.)
