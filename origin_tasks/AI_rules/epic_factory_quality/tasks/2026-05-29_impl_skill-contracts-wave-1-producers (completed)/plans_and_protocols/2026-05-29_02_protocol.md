---
skills_used:
  - claude-automated-mode
  - claude-route
  - task-resolve
  - claude-write-script
  - claude-modify-skill
  - verify-quality
  - task-complete
  - claude-commit
---

# Protocol — Wave 1 execution + prose-spec deletion audit trail (TASK-PROC-044-03)

**Date:** 2026-05-29 · **Session:** 86f3d13f (gmail, automated, Opus) · **Skill:** task-resolve

## What landed

### Schemas (`.claude/schemas/`)
- `scribble_metadata.yaml` — ported from prototype; reconciled to a SUPERSET of the live
  SKETCHES_README shape (added `date`, `screens_covered`; `version` accepts int or "v{N}";
  `contributing_requirements` notes the legacy singular `requirement:` form) so the README
  deletion loses no information (synthesis §8.8).
- `goal_metadata.yaml` — ported from prototype; added `started`, `source_exploration`, `bundle_id`.
- `flutter_handoff.yaml` — NEW (from SKETCHES_README flutter_handoff shape).
- `concept_canon_entry.yaml` — NEW (from concept_canon.yaml entry shape).
- `requirements_frontmatter.yaml` — NEW (from README Quick Reference + requirements_management spec).

### Producer contracts (full canonical field set: preconditions/postconditions + side_effects{target,action,note})
- `task-create/contract.yaml` (ported), `ui-create-scribble/contract.yaml` (ported),
  `requ-explore/contract.yaml` (NEW), `ux-write-canon-concept/contract.yaml` (NEW).
- Named-producer cross-refs the lint verifies within Wave 1:
  - task-create.derived_from requirements.md → skill:requ-explore ✓
  - requ-explore.derived_from goal.md → skill:task-create ✓
  - ui-create-scribble.derived_from requirements.md → skill:requ-explore ✓
  - ui-create-scribble.derived_from concept_canon.yaml → skill:ux-write-canon-concept ✓

### Lint — `scripts/quality/check_skill_contracts.py` (productionized via claude-write-script)
- Scans `.claude/skills/*/contract.yaml` (folder name = skill name).
- Named-producer verification ON (PoC skipped it): `source: skill:<name>` resolved against the
  named skill's produces by **basename** (robust to freeform placeholders; D-2 v1 risk).
  Managed producer with no matching artifact → ERROR; unmanaged producer → WARNING.
- Unannotated input: external-prefix or global produced-set fallback, else ERROR.
- may_invoke existence (SKILL.md/skill.md), schema-ref existence checks.
- Single summary WARNING line for the 56 unmanaged skills (not 56 lines).
- Tests: `scripts/tests/test_check_skill_contracts.py` (9 cases). Python gates G1/G2/G4/G5 PASS;
  G3's only failure is the pre-existing `test_orchestrate.py::TestBuildEnv::test_no_session_id_when_empty`
  baseline failure (orchestrate.py untouched) — not a regression.
- Deviation from synthesis D-5 (documented): a v1 skill may_invoke an unmanaged skill is NOT an
  ERROR (only SKILL.md existence is required). The strict version-0 may_invoke rule is deferred —
  enforcing it now would make Wave 1 unpassable (4 producers invoke ~10 still-unmanaged skills).

### verify-quality wiring (claude-modify-skill)
- §3.1 adds `CONTRACTS_CHANGED` detection; §3.3b runs the lint when contract/schema files change.
- Uses the existing `git diff --name-only HEAD` mechanism (consistent with DART/SCRIPTS detection):
  catches *modifications* to tracked contracts; brand-new untracked files are caught post-first-commit
  (pre-existing limitation of verify-quality's detection, not introduced here).

### Wave-1 fixture (claude-modify-skill)
- `ui-verify-flutter` gains an entry pre-check (5-line bash assertion) demonstrating the consumer
  runtime-check pattern Wave 2 generalizes.

## Prose specs DELETED / replaced this commit (atomic cleanup — AC)
D-5 names READMEs as the targets; only SHAPE duplication was removed, all narrative + a trimmed
illustrative example preserved.

| File | Section | Replaced by |
|---|---|---|
| `requirements_tasks/SKETCHES_README.md` | `metadata.yaml Format` exhaustive YAML block | pointer to scribble_metadata.yaml + trimmed example |
| `requirements_tasks/SKETCHES_README.md` | `flow_positions` YAML block | pointer (narrative bullets kept) |
| `requirements_tasks/SKETCHES_README.md` | `screen_versions` YAML block | pointer (narrative kept) |
| `requirements_tasks/SKETCHES_README.md` | stale `stale_since`/`pending_rules` field table | pointer (lifecycle narrative kept) |
| `requirements_tasks/SKETCHES_README.md` | `flutter_handoff.yaml` YAML block | pointer to flutter_handoff.yaml (narrative kept) |
| `requirements_tasks/README.md` | Quick Reference: requirement frontmatter YAML block | pointer to requirements_frontmatter.yaml |
| `requirements_tasks/README.md` | Quick Reference: task frontmatter YAML block | pointer to goal_metadata.yaml |
| `.claude/skills/ux-write-canon-concept/SKILL.md` | Add step 3 inline field enumeration | pointer to concept_canon_entry.yaml |

NOT deleted (intentional): the authoritative field POLICY in
`requirements_management/requirements_and_tasks/requirements.md` and `package_assignment_rules.md`
(shape vs. policy are different layers and coexist — documented in each schema's leading comment).

## Verification
- `python3 scripts/quality/check_skill_contracts.py` → PASS (4 contracts, 0 violations).
- All 9 schemas+contracts parse as valid YAML.
- Python gates: G1/G2/G4/G5 PASS; G3 only pre-existing unrelated baseline failure.
