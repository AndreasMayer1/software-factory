# Plan — Wave 1: Skill Contracts for Producer Skills (TASK-PROC-044-03)

**Date:** 2026-05-29 · **Session:** 86f3d13f-416d-46b9-ab86-c898e841a583 (gmail, automated, Opus)
**Skill route:** claude-route → task-resolve (doc/process impl — no `lib/` changes)

## Source design (read)
- `..._explore_skill-interface-contracts-mechanism (completed)/plans_and_protocols/2026-05-29_05_round_3_synthesis.md` (D-1..D-6, §7 honest list, §9 FU tasks)
- `.../2026-05-29_09_amendments.md` (A-1..A-8; precedence over Round 3)
- `.../prototypes/` (3 contracts, 2 schemas, 76-line lint, README, demo)

## Canonical contract field set (goal §2 + synthesis D-2)
`contract_version: 1`, `purpose:`, `derived_from:{required,optional}` (each item `source: external|skill:<name>` MANDATORY), `produces:{required,conditional}`, `quality_criteria:`, `may_invoke:`, `side_effects:[{target,action,note}]`, `preconditions:`, `postconditions:`.
> Note: prototypes used an older `side_effects:[{target, write}]` shape and omitted preconditions/postconditions. Wave-1 contracts use the FULL canonical set per goal.md.

## Deliverables & approach

### Phase A — 5 schemas (`.claude/schemas/`)
1. `scribble_metadata.yaml` — port + refine prototype
2. `goal_metadata.yaml` — port + refine prototype
3. `flutter_handoff.yaml` — NEW (from SKETCHES_README flutter_handoff section)
4. `concept_canon_entry.yaml` — NEW (from concept_canon.yaml entry shape)
5. `requirements_frontmatter.yaml` — NEW (from requirements_tasks/README §Quick Reference + requirements_management spec)
Each schema's leading comment declares which prose-spec sections it replaces.

### Phase B — 4 producer contracts
1. `task-create/contract.yaml` — port prototype to full field set
2. `requ-explore/contract.yaml` — NEW (produces requirements.md)
3. `ui-create-scribble/contract.yaml` — port prototype to full field set
4. `ux-write-canon-concept/contract.yaml` — NEW (produces concept_canon.yaml)

### Phase C — Atomic prose-spec cleanup (D-5; READMEs are the named targets)
- `requirements_tasks/SKETCHES_README.md`: replace "metadata.yaml Format", "flow_positions Field", "screen_versions Map", stale fields, and "flutter_handoff.yaml" shape blocks with `> See .claude/schemas/...`. Preserve all non-shape prose (lifecycle narrative, guidelines).
- `requirements_tasks/README.md` §Quick Reference: replace the two frontmatter code blocks with references to goal_metadata / requirements_frontmatter schemas.
- `ux-write-canon-concept` Add step field enumeration → reference concept_canon_entry schema.
All deletions land in the SAME task commit as the schemas (task-complete commits once) → satisfies the "same commit" atomicity AC.
Audit trail recorded in `02_protocol.md`.

### Phase D — Productionize lint (`scripts/quality/check_skill_contracts.py`)
Via `claude-write-script` (MANDATORY). Changes from PoC:
- Scan `.claude/skills/*/contract.yaml` (not a prototypes dir); load by folder name = skill name.
- Full **named-producer verification**: `source: skill:<name>` items must match a path in that named skill's `produces:` (required|conditional). PoC skipped this.
- `source: external` and known EXTERNAL prefixes skip cross-ref.
- `may_invoke` existence check (SKILL.md present).
- `contract_version: 0`/absent → WARNING (unmanaged), not ERROR.
- Exit 1 on violations with actionable messages.

### Phase E — Wire lint into verify-quality (`claude-modify-skill`)
Add §3.x to verify-quality: when `.claude/skills/*/contract.yaml` or `.claude/schemas/*.yaml` changed → run `python3 scripts/quality/check_skill_contracts.py`; non-zero = gate failure. (We edit the SKILL.md orchestration, NOT `check_quality_gates.sh` — respects the "don't edit gate-defining shell scripts" rule; the task itself authorizes the new lint.)

### Phase F — Wave-1 fixture pre-check (`claude-modify-skill`)
Add a 5-line `[ -f ... ]` pre-check at the top of `ui-verify-flutter` demonstrating the runtime-check pattern Wave 2 generalizes (asserts approved scribble metadata + schema presence).

### Phase G — Verify
Run the lint (expect PASS), run `check_python_gates.sh`, write audit-trail protocol.

### Phase H — Close
`doc-update-guidelines` → `verify-quality` → `task-complete`.

## Out of scope (later waves)
Consumer contracts + runtime pre-checks + `validate_against_schema.py` (FU-2); factory-map + token cost (FU-7); revision_target channel (FU-4). Canonical glob syntax deferred (D-2 v1 freeform).
