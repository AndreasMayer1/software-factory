# Plan — TASK-PROC-044-12: External-Boundary Contracts Rollout

**Date:** 2026-05-30 | **Author:** claude-route → task-resolve

## Source

- Exploration drafts: `2026-05-29_explore_external-interface-contracts (completed)/plans_and_protocols/02_external_contracts.md`
- Synthesis: `03_synthesis.md`
- Existing lint pattern: `scripts/quality/check_skill_contracts.py`
- External-state vocabulary: `scripts/factory/external_state/README.md`
- Verify-quality wiring pattern: `.claude/skills/verify-quality/SKILL.md` §3.3b

## Deliverables

### 1. `.claude/contracts/external/` (7 YAML files)
Canonical home for boundary contracts — outside `.claude/skills/` so check_skill_contracts.py's
`skills/*/contract.yaml` glob doesn't ingest them.

Files to create:
- `E1_developer_question_response.yaml` — from `02_external_contracts.md` E1 draft
- `E2_product_intake.yaml` — from analogy table (governance; `input_modality: conversation`)
- `E3_developer_notes.yaml` — E1 pattern (`file`), `file_exists_at_path`
- `E4_llm_web_research.yaml` — from `02_external_contracts.md` E4 draft
- `E5_os_tooling_install.yaml` — from `02_external_contracts.md` E5 draft
- `E6_dependency_admission.yaml` — E1 pattern, `developer_responded`
- `E7_code_release.yaml` — E5 pattern, `command_exited_zero` + `file_exists_at_path --min-bytes`
- `E9_optimize_event.yaml` — structured-file produce, `json_event_wellformed`

### 2. `.claude/schemas/pending_question.yaml`
Schema for the YAML frontmatter of `automation/pending_feedback/<TASK_ID>/question.md`.
Fields (from claude-automated-mode skill): task_id, session_id, account, status, asked_at, skill.

### 3. `.claude/schemas/external_contract.yaml`
Schema documenting the external-interface contract format including `input_modality:` field
(enum: file|frontmatter|conversation|invocation_arg|command_output|url_response, default: file).
This is the "schema dialect" extension for boundary contracts.

### 4. `scripts/quality/check_boundary_contracts.py`
Lint script (sibling to check_skill_contracts.py):
- Discovers `.claude/contracts/external/*.yaml`
- Validates `kind: external-interface`, `interface: E<n>`, `contract_version: 1`
- Validates `input_modality:` enum when present; absent → default `file` (no error)
- Validates each `quality_criteria[*].check` resolves to `scripts/factory/external_state/check_<term>.py`
- Validates `schema:` references exist
- Exit 0 = PASS, Exit 1 = FAIL (N violations)

### 5. `scripts/tests/test_check_boundary_contracts.py`
Tests (following test_check_skill_contracts.py pattern):
- Valid contract passes
- Unknown `check:` term → violation
- Missing schema reference → violation
- Missing `input_modality:` → defaults to `file` (passes, no violation)
- Invalid `input_modality:` value → violation
- Missing `kind:` → violation
- Wrong `contract_version:` → violation
- check_skill_contracts.py still passes with boundary contracts present

### 6. Update `verify-quality/SKILL.md`
- Extend `CONTRACTS_CHANGED` regex to include `.claude/contracts/external/*.yaml`
- Add gate §3.3c: boundary-contract lint (check_boundary_contracts.py)
- Update `verify-quality/contract.yaml` quality_criteria note

## Verification
- `check_skill_contracts.py` still passes (boundary contracts NOT ingested by its glob)
- `check_boundary_contracts.py` passes on all 7 authored contracts
- `bash scripts/quality/check_python_gates.sh` fully green
- All acceptance criteria from goal.md satisfied
