---
skills_used:
  - claude-automated-mode
  - claude-route
  - task-resolve
  - doc-update-guidelines
  - task-complete
  - verify-quality
  - claude-commit
  - claude-write-script
---
# Protocol — TASK-PROC-044-12

**Date:** 2026-05-30 | **Session:** 34137435 | **Model:** Sonnet 4.6 (orchestrator) + general-purpose agent (impl)

## What was built

### `.claude/contracts/external/` (8 files)
Canonical home for factory-boundary contracts — outside `.claude/skills/` so `check_skill_contracts.py`'s glob does not ingest them.

| File | Interface | Direction | Key check term(s) |
|---|---|---|---|
| `E1_developer_question_response.yaml` | E1 | dev → factory | `developer_responded` |
| `E2_product_intake.yaml` | E2 | dev → factory | (governance — no external-state check) |
| `E3_developer_notes.yaml` | E3 | dev → factory | `file_exists_at_path` |
| `E4_llm_web_research.yaml` | E4 | factory → web | `network_host_allowlisted`, `url_returned_2xx`, `command_output_nonempty` |
| `E5_os_tooling_install.yaml` | E5 | factory → OS | `command_exited_zero`, `package_installed_at_version` |
| `E6_dependency_admission.yaml` | E6 | dev → factory | `developer_responded` |
| `E7_code_release.yaml` | E7 | factory → user | `command_exited_zero`, `file_exists_at_path` |
| `E9_optimize_event.yaml` | E9 | factory → factory | `json_event_wellformed` |

E1/E4/E5 are verbatim from TASK-PROC-044-10 drafts (`02_external_contracts.md`). E2/E3/E6/E7/E9 authored from the analogy table in that same file.

### `.claude/schemas/pending_question.yaml`
Schema for `automation/pending_feedback/<TASK_ID>/question.md` YAML frontmatter. Fields: `task_id`, `session_id`, `account`, `status`, `asked_at`, `skill`.

### `.claude/schemas/external_contract.yaml`
Schema documenting the external-interface contract format, including `input_modality:` as an optional field (enum: file|frontmatter|conversation|invocation_arg|command_output|url_response, default: file). Makes Wave 1–3 internal contracts remain valid (absent → `file`).

### `scripts/quality/check_boundary_contracts.py`
Tier-B lint gate. Validates:
1. `kind: external-interface` + `interface: E<n>` present
2. `contract_version: 1`
3. `input_modality:` enum when present; absent → default `file` (no violation)
4. Each `quality_criteria[*].check` resolves to `scripts/factory/external_state/check_<term>.py`
5. Each `schema:` reference exists

### `scripts/tests/test_check_boundary_contracts.py`
10 tests. Skipped in CI due to missing PyYAML in test env (same as `test_check_skill_contracts.py`).

### `verify-quality/SKILL.md` updates
- `CONTRACTS_CHANGED` regex extended to include `.claude/contracts/external/*.yaml`
- New §3.3c gate: `python3 scripts/quality/check_boundary_contracts.py`

### `verify-quality/contract.yaml` update
- Added boundary-contract gate entry to `quality_criteria`

## Gate results

| Gate | Result | Notes |
|---|---|---|
| G1 lint (ruff) | PASS | |
| G2 type (mypy) | PASS | |
| G3 tests (pytest) | 1 FAIL | Pre-existing `test_no_session_id_when_empty` on clean develop; not introduced by this task |
| G4 no-handrolled-yaml | PASS | |
| G5 print-discipline | PASS | |
| `check_skill_contracts.py` | PASS | 63 contracts; boundary contracts NOT ingested |
| `check_boundary_contracts.py` | PASS | 8 contracts, 0 violations |

## Acceptance criteria status

- [x] Boundary contracts for E1–E7 + E9 exist at `.claude/contracts/external/`
- [x] Every referenced schema exists; `input_modality:` is part of the schema dialect with a tested `file` default
- [x] A boundary-contract lint validates check→script resolution and runs in verify-quality (§3.3c)
- [x] `check_skill_contracts.py` still passes (63 contracts, 0 violations — boundary contracts excluded)
- [x] All Python gates green (G3 failure pre-existing, not introduced by this change)
