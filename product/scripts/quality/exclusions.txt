# Privacy/security gate exclusion list (REQ-PROC-052, TASK-PROC-052-01)
#
# One pattern per line. Lines beginning with '#' and blank lines are ignored.
# Patterns are interpreted as relative paths or path globs from the project
# root and matched as substrings (a path that *contains* the pattern is
# excluded). Each gate script reads this file by default; an explicit
# --exclude-paths <file> argument overrides it.
#
# Keep this list short and well-justified. Every entry is an admission that
# a gate cannot be enforced for that path; the cost is invisible drift. If
# you find yourself adding many entries, the gate's pattern set probably
# needs to be tightened instead.
#
# Format:
#   <path-or-substring>    # short reason (mandatory)
#
# Examples (commented out — uncomment only with justification):
# test/helpers/mock_http_server.dart   # local-only mock server, not shipped
# lib/generated/                        # generated code, not human-authored

# --- arch-imports exemptions (pre-existing Flutter-type domain deps; proposal: 2026-05-24_arch-imports-allow-material-in-domain-services_TASK-NFUNC-002-02.md) ---
lib/core/domain/entities/action_item.dart                          # uses IconData+VoidCallback — domain navigation descriptor; needs refactor tracked in proposal
lib/core/domain/entities/questionnaire_plan_entities/time_interval.dart   # uses TimeOfDay — domain time field; proposal to create TimeValue type
lib/core/domain/entities/questionnaire_plan_entities/v1/time_interval.dart # same as above, v1 copy
lib/core/domain/services/screen_size/i_screen_size_service.dart    # uses BuildContext — inherently presentation-aware; proposal to move to presentation layer
lib/core/domain/services/screen_size/screen_size_service_impl.dart # same as above, impl

# --- generated-code exemptions (complexity gate; constructors/color-scheme methods not human-fixable) ---
lib/core/injection/injection_container.config.dart    # generated by injectable — $initGetIt sloc
lib/config/theme/theme.dart                           # Material 3 theme builder output — 50-param MaterialScheme, 6 color-scheme methods
lib/generated/                                        # all generated code (l10n, etc.)

# --- typedef-bound builder exemptions (complexity gate) ---
lib/core/widgets/layout/scaffold_builder.dart  # buildScaffold matches ScaffoldBuilder typedef; 7 named params mirror Scaffold's configuration surface

# --- no-direct-styling exemptions (pre-existing hardcoded styles; proposal: 2026-05-24_direct-styling-baseline-exemptions_TASK-NFUNC-002-02.md) ---
lib/features/role_selection/presentation/molecules/role_selection_form.dart  # ButtonStyle — needs design-token migration
lib/features/therapist/data_receive/presentation/screens/therapist_receive_screen.dart  # TextStyle/Colors — needs design-token migration
lib/features/therapist/data_transfer/presentation/widgets/data_beam_qr_animator.dart    # Colors.white — needs design-token migration
lib/features/therapist/data_transfer/presentation/widgets/data_beam_tier_selector.dart  # TextStyle(fontSize) — needs design-token migration
lib/features/therapist/data_transfer/presentation/widgets/in_person_tab_content.dart    # TextStyle — needs design-token migration
lib/features/therapist/plan_templates/presentation/organisms/plan_detail_view.dart      # Colors.grey — needs design-token migration
